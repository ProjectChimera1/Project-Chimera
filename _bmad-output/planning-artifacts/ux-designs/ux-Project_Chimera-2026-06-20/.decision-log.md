# UX Decision Log — Project Chimera (run 2026-06-20)

Canonical record of decisions for this UX run. Spines (`DESIGN.md`, `EXPERIENCE.md`) are distilled from this log + imports + sources. Spines win on conflict.

## Run metadata
- **Date:** 2026-06-20
- **User:** Alec
- **Mode:** Create — **fresh run**, inherits the prior in-progress run `ux-Project_Chimera-2026-06-05/` (which was design-handoff, never finalized).
- **Position in chain:** Step 1 of the full GDS planning chain (UX → forward architecture → epics/stories → re-run readiness → sprint planning), per `implementation-readiness-report-2026-06-05.md` (🔴 NOT READY: no UX/epics/stories).
- **Sources:** `Project_Chimera_GDD.md`; PRD `prds/prd-Project_Chimera-2026-06-05/prd.md` (60 FRs, 6 NFRs, UJ-1..UJ-6); `Snapshot.md`; `_bmad-output/project-context.md`.

## D1 — Visual identity is DECIDED and shipped (not re-elicited)
The "Claude Design UI" at `ux-Project_Chimera-2026-06-05/mockups/project-chimera/` is the **latest** (confirmed by Alec, 2026-06-20) and is already partly implemented in the Godot HUD. It is the authoritative visual source. This run **distills** it into `DESIGN.md`, it does not redesign it.
- **Why:** Alec produced the UI in Claude Design and is shipping it (see project memory `ui-ship-finished-not-redesign`). Re-eliciting would discard approved work.

## D2 — Design language (from `chimera.css`)
- **Low-poly echo, faceted dark UI.** Chamfered corners via `clip-path` polygons (`--cut` 8/5/14px), thin cel-shade top-edge highlight, flat color blocks — UI chrome echoes the low-poly 3D art so UI + world read as one piece.
- **HUD is non-diegetic** flat overlay (SC2/classic-RTS convention) — clarity over in-world illusion.
- **Accent = teal default**, with `amber` and `violet` accent variants (`[data-accent]`). Dark theme default; a full **light theme** (warm paper) exists (`[data-theme="light"]`).
- **Type:** Chakra Petch (display/headings/buttons, uppercase + letter-spacing), Space Grotesk (UI/body), JetBrains Mono (numbers/readouts, tabular-nums). 1.250 scale, 11→72px.
- **Faction/team colors are sacred + reserved** — 8 **Okabe-Ito-derived, colorblind-safe** team colors live on units; UI accents must never be confused for them.
- **Chimera Seal mark** — geometric alchemical "transmutation sigil" (two rings + fire/water triangles + nucleus) + a **transmute spinner** loading indicator. Monochrome, engraves on any surface.

## D3 — Alchemical motif scope
The shipped kit retains a **light alchemical motif** (the Chimera Seal mark + transmute-spinner naming). This is distinct from — and survives — the broader **bio-alchemy "Transmutation Lab" retheme that was explored then SHELVED** (recoverable at git `8265cdf`; see memory `ui-ship-finished-not-redesign`). Keep the mark/spinner; do **not** reintroduce the full retheme.

## D4 — Scope of this run
1. **Finalize the spines** from the shipped UI: `DESIGN.md` (tokens + components, done this run) and `EXPERIENCE.md` (IA, states, flows, input, accessibility — from the screen mockups).
2. **Design the gap surfaces** the readiness report named (no mockups yet): Unit Card Editor (FR-2), Ability Editor (FR-8–12), Building/Tech-Tree visual editor (FR-13–14), Faction Definer 5-step wizard (FR-17), Trigger T2/T3 progressive-disclosure editors (FR-23–28), Save/Load hero-picker (FR-7d/e), creator-authored custom runtime UI (FR-26).
3. **Make NFR-1/2/3 concrete + acceptance-testable:** NFR-1 fast edit→play (no restart, ≤ a few sec); NFR-2 tooltip on every control + "Your First Scenario" onboarding + <15-min first scenario; NFR-3 editor invisible to non-creator "Commanders".

## Open items (pending Alec)
- [ ] Flows/wireframes for the 7 gap surfaces — working mode is Fast path (I draft with `[ASSUMPTION]`, Alec reacts).
- [ ] Confirm DESIGN.md distillation is faithful before EXPERIENCE.md + gap surfaces build on it.
- [ ] NFR-1/2/3 acceptance criteria wording.
