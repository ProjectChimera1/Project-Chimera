# Finished UI — Claude Design mockups

This folder holds the **canonical, finished UI** for Project Chimera — the design we're implementing
against (the "old finished UI"). Drop the Claude Design export **zip here and extract it**.

- Implementation targets these mockups.
- The design system that describes them lives in `../DESIGN.md` (visual tokens) and `../EXPERIENCE.md`
  (behavior), with the source handoff in `../design-handoff-prompt.md`.
