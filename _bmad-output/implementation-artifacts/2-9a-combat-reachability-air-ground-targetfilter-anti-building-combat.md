# Story 2.9a: Combat reachability — air/ground TargetFilter + anti-building combat

Status: ready-for-dev

<!-- Validation: optional. Created via gds-create-story [ultracode]: 6-analyst parallel live-source recon (combat target-selection · entityworld air-tag · buildingstore-as-target · determinism/goldens · data-defs/TargetFilter · continuity/wire/arch) + direct source grounding + a 3-validator adversarial panel whose findings were applied. -->

## Story

As a player,
I want units to honor anti-air / anti-ground target filters and to be able to attack enemy buildings,
So that the showcase factions' combat resolves against the right targets and armies can destroy structures.

## Why this matters (source-verified)

Today `CombatSystem` picks targets **purely by nearest-enemy**: the only filter in the acquire loop is `if (world.FactionOf[j] == myFaction) continue;` (`SpatialHash.cs:107`, mirrored at `:139`). There is **no air/ground distinction and no unit/building distinction**, and damage resolves **only against `EntityWorld`** (`DamageResolver.Apply` is hard-bound to `world.Health[t]` / `world.EffectiveArmor[t]` / `world.Destroy(t)`). The design doc flags both gaps as **needs-code**:

- Anti-air: *"CombatSystem ignores Air/Ground entirely — anyone in range is a valid target. True anti-air discrimination (and flyer-only / can't-hit-air rules) needs D1 TargetFilter plus a CombatSystem rewire to honor it."* (`fma-faction-design.md:171`)
- Anti-building: *"Projectile/splash code only iterates EntityWorld units, not BuildingStore. Both sieges' anti-structure role does not function today … Needs ApplyHit/ApplySplash to target buildings."* (`fma-faction-design.md:112,124,173`) — **Siege units are the intended anti-structure counter**; buildings are currently **indestructible in-match** (the only `BuildingStore.Destroy` caller is the editor delete tool, `EntityPlacer.cs:1029`).

Story 2.8 made the full rosters (incl. the Air units `griffin`/`wyvern`) trainable. 2.9a makes their combat **correct**: air units get properly countered, and armies can finally raze an enemy base. This unblocks **2.10** (showcase signature mechanics) and **2.9b** (worker-cast / crystal cost), both of which depend on 2.9a.

_Covers: FR-11, FR-12, AR-8, NFR-4. Depends on: 2.4 (ability/command-card spine — done), 2.8 (Air building + `OrderApplier` `BuildingSystem?` threading — done)._

## Acceptance Criteria

### AC1 — Air/ground target filter in auto-acquire (epic AC1, verbatim + made testable)

**Given** a unit whose attackable-domain set excludes a domain, and both a matching and a non-matching enemy in range **When** `CombatSystem` selects a target **Then** it engages **only** an enemy whose domain the unit may attack — an anti-air-only unit (`attack_domains: ["Air"]`) engages only the flying enemy (`CategoryOf == Air`) and a ground-only unit engages only the ground enemy — **And** target selection remains deterministic and **ascending-id stable** (the filter only prunes candidates before the existing strict-nearest compare; ties still resolve to the lowest-id candidate).

- **AC1.1** The domain filter is applied in **both** acquire paths — `SpatialHash.FindNearestEnemy` (in-range, `:107`) **and** `FindNearestEnemyGlobal` (idle chase-to-contact, `:139`) — so an anti-air unit with no valid air target does **not** march toward a ground enemy it can never damage.
- **AC1.2** "Is the candidate flying?" is read from **`EntityWorld.CategoryOf[j]`** (`UnitCategory.Air == 4`), never from `armor_type` (the two shipped air units have **different** armor: `griffin`=Light, `wyvern`=Medium — armor cannot identify air).
- **AC1.3** The default (unauthored) attackable-domain set is **All** — every existing unit attacks every domain exactly as today. No shipping faction JSON is edited; the AC1 restricted-unit case is exercised with a **synthetic test unit**, so no golden moves (see AC3).
- **AC1.4** A unit's own domain (`CategoryOf`) does **not** restrict what it can attack; the new `attack_domains` field is a distinct **attacker-capability** axis. (An Air unit with default `All` still hits ground; an `["Air"]` ground unit hits only fliers.)

### AC2 — Anti-building combat, explicit-order-only (epic AC2, verbatim + made testable)

**Given** a combat unit **ordered** to attack an enemy building **When** it is in range **Then** it deals damage to that building in `BuildingStore` via the **DamageMatrix** (`amount × DamageTable.Get(attackerDamageType, ArmorType.Fortified)`, floored at 0) and can **destroy** it (`Health ≤ 0 → BuildingStore.Destroy`) **And friendly buildings are never targeted** (`buildings.FactionOf[b] == attackerFaction` is rejected **in-tick at combat-resolve time, before any damage** — mirroring how `AttackTarget` validates its target in-tick).

- **AC2.1** Anti-building is **explicit-order-only**: idle/auto-acquire never targets buildings (buildings are not in the combat `SpatialHash`; nothing is added to it). This is a determinism requirement — see AC3.
- **AC2.2** Building damage uses the **matrix path** (not `DirectHpDelta`): a Siege attacker (Siege vs Fortified = 1.5×) razes structures fast; a Pierce attacker (Pierce vs Fortified = 0.25×) barely dents them — matching the intended counter design.
- **AC2.3** An out-of-range attack-building order makes the unit **chase** to the building's `Position` (Fixed center point) and then fire when in `AttackRange`; on the building's death the attacker reverts to Idle. Cooldown gating (`AttackCooldown`/`AttackSpeed`) is honored exactly as unit-vs-unit.
- **AC2.4** A unit whose `attack_domains` excludes **Structure** does **not** damage a building when ordered onto one — the in-tick guard reverts it to Idle before any chase/attack (a no-op, no attack spent). Default `All` includes Structure, so every existing combat unit can attack buildings when ordered.
- **AC2.5** Ordering an attack on a building **under construction** is allowed (standard RTS; `IsUnderConstruction` is not a shield). A **Neutral** building is a valid target when explicitly ordered (unit combat already treats Neutral as hostile).
- **AC2.6** Damage resolution **mirrors unit-vs-unit** (D-4): a **melee** attacker (`AttackRange ≤ 2.5`) hits the building **instantly**; a **ranged** attacker fires a **real projectile** that flies to the building and applies matrix damage on impact via `ProjectileSystem` (reusing `ArmorType.Fortified` + the shared damage helper). A building that dies while a shell is in flight drops the projectile harmlessly.

### AC3 — Determinism & zero regression (explicit; prevents "completion lies")

**Given** the change set **When** the Tier-1 golden gate runs **Then** all **10** golden checksums are **byte-identical**, `SimChecksum.AlgoVersion` stays **8**, `CanonicalModelHash` AlgoVersion stays **2**, the known-state pin `0x983D39AE` is unchanged, and `VersionStampConsistencyTests` (8/2/1/2 + "0.1") passes. **And** `ApplyUnitDefinitionGuardTest` passes with the new `AttackDomainOf` field asserted. **No fold, no AlgoVersion bump, no golden re-record.**

- **AC3.1** The new `AttackDomainOf` per-unit field is **authored-immutable → NOT folded** (the `DamageTypeOf`/`ArmorTypeOf`/`CategoryOf` posture — all combat-read authored fields, none folded; a divergent authored choice is caught transitively when the differing target's already-folded `Position`/`Health` move).
- **AC3.2** Anti-building damage writes `buildings.Health` / `buildings.Alive`, which are **already folded** (`SimChecksum.cs:185-186`) — **no new fold**. No new folded SoA field is introduced for the building target (the building id rides the already-folded `CommandTarget` under `CommandState == AttackBuilding`).
- **AC3.3** No golden moves: **no golden scenario spawns an Air-category unit** (only `"Worker"` appears, in `GoldenApplierScenario`) so the default-`All` filter is inert everywhere; and the 3 goldens that place buildings (`golden-scenario`, `golden-multifaction`, `ai-active`) **never issue an attack-building order**, so explicit-order-only leaves them untouched.
- **AC3.4** New coverage lands as **new** golden(s) — a `combat-air-ground` filter golden, an `anti-building` destruction golden (run long enough for the ranged shell to land), and an `ability-domain-filter` golden (AC6) — never by editing the existing 10.
- **AC3.5** The two scope expansions are fold-neutral too: the ability-side domain filter (AC6) reads authored `CategoryOf` in `TargetMatcher` (no fold; "no domain bit = all domains" keeps every existing `SearchArea` byte-identical), and the ranged-vs-building projectile (D-4) rides `ProjectileStore`, which is **not** a `SimChecksum.Compute` input (no fold; the building-target flag defaults to entity-target so no existing golden changes).

### AC4 — MP-deterministic anti-building (the order rides the lockstep wire)

**Given** two peers (or a live match and its `.chmr` replay) **When** a player orders a unit to attack an enemy building **Then** the action rides the deterministic command stream as a new `UnitCommand.AttackBuilding = 12` through the **shared `OrderApplier.Apply`**, applied identically on every peer and in replay, with **no wire/replay/version bump** (rides the existing 11-byte `UnitOrder`; `ReplayRecorder.VERSION`/`PROTOCOL_VERSION` unchanged — the `Train`/`CastAbility` precedent). **And** all **three** apply sites (`LockstepManager` / `ReplayPlayer` / offline `SelectionSystem`) stay one switch — `AttackBuilding` is a **pure entity command** (attacker in `UnitId`) that writes only `EntityWorld` SoA (stores the building id in `CommandTarget`), so it needs **no `BuildingSystem`/`BuildingStore` param** in the apply path and the offline `SelectionSystem` call is **identical to its `AttackTarget` call** (AR-17 structural parity holds for free, zero new plumbing). **And** the attacker-ownership guard (own-unit-only, `NetworkCommand.cs:135-137`) is preserved; the friendly-building guard **and** the `Structure`-domain gate are enforced **deterministically in-tick, before any damage** (in `TickAttackBuildingCombat`, mirroring how `AttackTarget` validates its target in-tick, not at apply).

### AC5 — UX (right-click an enemy building issues the attack)

**Given** a selection of the player's combat units **When** the player right-clicks an enemy building **Then** an attack-building order is issued (the right-click dispatch resolves an enemy **building** pick between the existing enemy-**unit** pick and the Move fallback), and the units move to and destroy it. `/godot-verify` confirms the picker, the chase, matrix damage, destruction, and the friendly-building no-op in-engine.

### AC6 — Ability `SearchArea` domain filter (closes the reserved `TargetFilter` bits, D-1)

**Given** an ability effect with a `SearchArea` whose `TargetFilter` sets a domain bit (`Air`/`Ground`/`Structure`) **When** the effect executes **Then** `TargetMatcher` includes only candidates whose `CategoryOf`-domain matches (an `Air`-filtered heal touches only flyers; a `Ground`-filtered nuke spares air), evaluated via the **same** `CategoryOf → domain` classifier as AC1 **And** an authored ability carrying those bits **passes** validation (the fail-closed reject at `EffectNodeJsonConverter.cs:246` is lifted) and the bits appear in the ability-editor's filter vocabulary (`DraftVocabulary.Filters` + the filter UI).

- **AC6.1** "No domain bit set" = **all domains** (the reserved bits were ignored before, so an empty domain set must still match everything), so every existing `SearchArea` (domain=0) behaves **byte-identically** — no golden moves.
- **AC6.2** The guarded ability tests are updated to the new contract: `ReservedTargetFilterBit_IsLocatedReject` is **inverted** (the bits are now accepted + evaluated), and `FlagCombinationVocabularyTests` includes the 3 domain bits. A new `ability-domain-filter` golden covers a domain-filtered `SearchArea`.
- **AC6.3** Still deterministic + no fold: `TargetMatcher` reads authored `CategoryOf` (not folded); the effect's mutations land on already-folded `Health`/modifiers. `AlgoVersion` stays 8.

## Decisions

**Baked in (deterministic-rule / minimal-slice calls — applied, not re-asked):**

1. **Anti-building is EXPLICIT-ORDER-ONLY (not auto-acquire).** Buildings are not in the combat `SpatialHash` (entity-only buffers), so idle units cannot auto-target them without adding building candidates to the hash — which would change `golden-scenario`/`golden-multifaction`/`ai-active` (their surviving enemy units would march on the nearby enemy buildings) and force a 10-golden re-record. Explicit-order-only keeps every golden byte-identical **and** matches the epic wording ("a combat unit **ORDERED** to attack an enemy building"). Auto-acquire of buildings (arrived AttackMove waves picking a base) is a deliberate **later** enhancement (see `deferred-work.md` item 7), out of scope here.
2. **`AttackDomain` default = All.** A new authored-immutable field defaulting to `All` means existing faction JSON needs **zero** edits, `CombatSystem` behavior is unchanged, and all 10 goldens stay byte-identical. Author opt-in **restriction** only. Set in `EntityWorld.Create()` (recycle-slot safety) and written via `ApplyUnitDefinition`.
3. **No fold, `AlgoVersion` stays 8.** `AttackDomainOf` is authored-immutable (`DamageTypeOf`/`ArmorTypeOf`/`CategoryOf` posture → not folded); building `Health`/`Alive` are already folded → anti-building adds no fold; the persistent building target reuses the already-folded `CommandTarget` (no new folded SoA). ([[chimera-checksum-fold-timing-rule]])
4. **`UnitCommand.AttackBuilding = 12`** (append after `Train=11`; values 0–11 frozen for replay). Pack the **attacker entity id in `UnitId`** (so the existing entity-ownership guard at `NetworkCommand.cs:135-137` validates it for free) and the **building id in `TargetX`** as a raw int (`Fixed.FromRaw` at issue, read as `o.TargetX` — never `.ToFloat()`). The case goes **inside** the switch **after** the ownership guard (unlike `Train`, which names a building in `UnitId` and is handled before the guard). **`OrderApplier` sets `CommandState[id] = o.Command` generically at `NetworkCommand.cs:140`** (after the guard, before the switch), so the case only **blind-stores** the target — `CommandTarget[id] = buildingId`; `AttackTarget[id] = -1`; clear the `Attacking` flag + `ClearPatrolRoute` — **exactly the `AttackTarget` model, which validates its target in-tick, not at apply**. **All** building validation (bounds `b < Count`, `Alive`, friendly-`FactionOf`, `Structure`-domain) happens **in-tick** in `TickAttackBuildingCombat`, which reverts to Idle on any failure — so the apply case needs **no building reference at all** (no `buildings` param, no offline plumbing). `CommandState` disambiguates which store `CommandTarget` indexes (mirrors `AttackTarget`/`Follow` sharing `CommandTarget`). **Do NOT reuse `AttackTarget=6`** (it reads `CommandTarget` as an **entity** id), and **do NOT copy `AttackTarget`'s `AttackTarget[id]=o.TargetX` line** (a building id in the entity-space `AttackTarget[]` is the exact hazard the design forbids) — set `AttackTarget[id] = -1`.
5. **`AttackDomain` includes a `Structure` bit; the ordered attack-building is gated on it.** Default `All` (Ground|Air|Structure) so every existing combat unit can be ordered to attack buildings; a creator authoring `attack_domains: ["Air"]` gets an AA-only unit that also cannot hit buildings (AC2.4).
6. **AC1 demo uses a synthetic test unit**, not a shipping-roster edit — editing a fielded unit could move a golden; a synthetic `["Air"]` unit in a dedicated test scenario moves nothing. (There is no dedicated AA unit in either roster today; the design doc designates the `archer`/Pierce Marksman as the anti-air *finisher* but nothing in data expresses targeting — a fielded AA unit is a **content** decision for later, kept off every golden scenario.)

**Needs Alec's confirmation (recommended defaults baked so the dev can start; see the summary after this file):**

- **D-1 (SCOPE — ability-side reserved bits): INCLUDE in 2.9a — RESOLVED (Alec, 2026-07-01: "Include it in 2.9a").** 2.9a ALSO closes the **ability-side** `SearchArea` domain filter: the reserved `Effects.TargetFilter.Air/Ground/Structure` bits (stamped "reserved for Story 2.9a") get **evaluated** in `TargetMatcher` via the **same** shared `CategoryOf → domain` classifier AC1's combat filter uses; the fail-closed reject at `EffectNodeJsonConverter.cs:246` is **lifted**; the bits are added to `DraftVocabulary.Filters` (`AbilityDraft.cs:53`) + surfaced in the ability-editor filter UI; and the guarded tests are updated (invert `ReservedTargetFilterBit_IsLocatedReject`, extend `FlagCombinationVocabularyTests`). See **AC6** + **Task 11**. This is a distinct mechanism from AC1 (an ability *target predicate*, not an *attacker capability*), so it uses the **existing `TargetFilter` enum** (its native home) while AC1's per-unit field uses the new `AttackDomain` enum (D-2) — the two share only the classification helper. Determinism-safe: `TargetMatcher` reads authored `CategoryOf` (no fold), and **"no domain bit set = all domains"** preserves every existing `SearchArea`'s behavior byte-identically (existing filters carry domain=0).
- **D-2 (reuse): NEW Core `AttackDomain` flags enum — do NOT reuse `Effects.TargetFilter`.** The `Effects` Air/Ground/Structure bits are **actively rejected** on the ability path and absent from the composer vocabulary — the *same value* would be "forbidden" for abilities yet "valid" on a unit field (split-personality enum). And it is a different axis (attacker capability vs ability target predicate; the allegiance bits are meaningless on a unit's attack-capability field). Recommended: `[Flags] enum AttackDomain : byte { None=0, Ground=1<<0, Air=1<<1, Structure=1<<2, All=Ground|Air|Structure }` in `src/Core`, with a shared `CategoryOf → domain` classifier both this and any future ability-side unlock can consume. (`game-architecture.md:415` envisions one shared TargetFilter vocabulary long-term; the shared classifier is the honest reconciliation without importing the ability enum's baggage.)
- **D-3 (building armor): hardcode `ArmorType.Fortified` at the damage call.** **100%** of shipped building JSON authors `armor_type: "Fortified"`, and `BuildingStore` has no `ArmorType` field (`Create` ignores building JSON entirely today). Recommended: pass the `Fortified` constant (zero new SoA, no fold, correct for all current data). The creator-extensible upgrade — `BuildingStore.ArmorType[]` fed from `def.ParsedArmorType` at placement — is noted out-of-scope (it requires `BuildingStore.Create` to start reading the def and belongs with a larger data-driven-building arc).
- **D-4 (ranged-vs-building): FIRE A REAL PROJECTILE for ranged attackers — RESOLVED (Alec, 2026-07-01: "Fire a real projectile").** `TickAttackBuildingCombat`'s fire step **mirrors `TryDealDamage`'s melee/ranged split** (`CombatSystem.cs:465-504`): a **melee** attacker (`AttackRange ≤ MELEE_THRESHOLD 2.5`) resolves instant matrix damage via the Task-4 helper; a **ranged** attacker spawns a **projectile** that flies to the building and resolves on impact (like unit-vs-unit ranged). This needs a **building-target discriminator on `ProjectileStore`** (`TargetId` is an entity id today, `:21`) + `BuildingStore` threaded into `ProjectileSystem` (`SimulationHost.cs:128`) + `ProjectileSystem.ApplyHit` resolving a building hit via the shared helper (`ArmorType.Fortified`). See **Task 4b**. Determinism-safe: `ProjectileStore` is **not** folded (`SimChecksum.Compute` takes `EntityWorld`/`BuildingStore`/`ResourceStore`/`FactionRegistry`/`ModifierStore` — **not** projectiles), so the new discriminator adds no fold; existing goldens are byte-identical (the flag defaults to entity-target; no existing golden spawns a building-target projectile).

## Tasks / Subtasks

- [ ] **Task 1 — `AttackDomain` enum + `attack_domains` authored field (sim data; no behavior change at default)** (AC: 1, 1.3, D-2)
  - [ ] Add `[Flags] public enum AttackDomain : byte { None=0, Ground=1<<0, Air=1<<1, Structure=1<<2, All=Ground|Air|Structure }` in `godot/src/Core/` (new file `AttackDomain.cs`, sibling to `UnitCategory.cs`). Pure C#, no Godot.
  - [ ] Add a shared **classifier** for target-side classification, single-sourced: map a target's `UnitCategory` to its domain (Air→air, Structure→structure, else ground). **Both** consumers use it — the combat filter (AC1, against an `AttackDomain` mask) **and** the ability `SearchArea` filter (AC6, against the `TargetFilter` `Air/Ground/Structure` bits). Because the two consumers use different enums (`AttackDomain` vs `TargetFilter`), the clean shape is a neutral 3-valued `Domain` that each side maps to its own bit (or two tiny `DomainOf`/`DomainBitOf` switches) — keep the `CategoryOf → domain` **logic** single-sourced so the two filters can never disagree on what "a flyer" is.
  - [ ] `UnitDefinition.cs`: add `[JsonPropertyName("attack_domains")] public string[]? AttackDomains { get; set; }` (nullable; omitted/`[]`/all-three ⇒ default) mirroring the `prerequisites`/`abilities` snake_case string-array convention (`:117-127`), plus a `[JsonIgnore] public AttackDomain ParsedAttackDomains` accessor that OR-folds the strings (`"Ground"`/`"Air"`/`"Structure"`, exact-string switch like `ParsedCategory` `:255-263`) and returns `AttackDomain.All` when null/empty/unknown-only. Fully data-driven — a creator edits one field.

- [ ] **Task 2 — `EntityWorld.AttackDomainOf` SoA via the single mapper (A2 rule)** (AC: 1, 1.3, 1.4, 3, 3.1)
  - [ ] Add `public readonly AttackDomain[] AttackDomainOf;` to `EntityWorld` (allocate `MAX_ENTITIES`, next to `CategoryOf` `:246`). Comment it **NOT folded (authored-immutable, `CategoryOf`/`DamageTypeOf` posture)**.
  - [ ] Write it in `ApplyUnitDefinition` (`:582/605`) from `def.ParsedAttackDomains` — the single def→SoA mapper. **Never hand-copy in a spawn path** (the 1.12/1.13 zombie-slot defect class).
  - [ ] Default it to `AttackDomain.All` in `Create()` (`:479/521-530`) so recycled slots and any non-def path (RestoreUnit, workers) attack every domain exactly as today. (Workers never auto-attack — `EffectiveAttackDamage==0` skip at `CombatSystem.cs:103` — so `DoSpawnWorker`'s hand-copy block at `EntityPlacer.cs:452-455` does **not** need this field; the `Create` default covers it.)
  - [ ] Extend `ApplyUnitDefinitionGuardTest` (`:44-127`): set a **non-default** `AttackDomains` on the `CombatDef()` fixture and add `Assert.Equal(def.ParsedAttackDomains, w.AttackDomainOf[id])` + a `Assert.NotEqual(AttackDomain.All, …)` in **both** guard tests (the guard hard-lists each field — a forgotten assertion gives silent zero coverage). **Do NOT** add it to `SimChecksumCoverageGuardTest` (it is deliberately unfolded).

- [ ] **Task 3 — Honor the filter in both acquire paths (sim)** (AC: 1, 1.1, 1.2, 3.3)
  - [ ] Add a helper `bool CanAttack(AttackDomain attackerDomains, UnitCategory targetCategory)` = `(attackerDomains & DomainOf(targetCategory)) != 0` (Fixed-free integer bit-AND). Place it where both `SpatialHash` acquire methods can call it (they already take `world`+`id`, so read `world.AttackDomainOf[id]` and `world.CategoryOf[j]` — no signature change).
  - [ ] Insert `if (!CanAttack(world.AttackDomainOf[id], world.CategoryOf[j])) continue;` immediately **after** the existing faction filter in `SpatialHash.FindNearestEnemy` (`:107`) **and** `FindNearestEnemyGlobal` (`:139`). This only prunes candidates before the strict-nearest compare — ascending-id stability preserved, no `Dictionary`/`HashSet`, no float.
  - [ ] Confirm no other acquire entry point exists (grep `FindNearestEnemy`); `TickIdleCombat`/`TickStationaryCombat`/`TickAttackMoveCombat`/`TickPatrolCombat` all funnel through these two, so their bodies stay untouched.

- [ ] **Task 4 — Thread `BuildingStore` into `CombatSystem` + shared building-damage helper (sim)** (AC: 2, 2.2, 3.2, D-3, D-4)
  - [ ] Extend the `CombatSystem` ctor (`:50`) to accept a **`BuildingStore? buildings = null`** (the store, **not** `BuildingSystem` — the tick only needs the public `Alive`/`FactionOf`/`Position`/`Health`/`Count`/`Destroy` members, all on `BuildingStore`; nullable so headless/unit-test paths pass null and building-attacks no-op). Pass `Buildings` at the construction site `SimulationHost.cs:126` (system index [5]; `Buildings` (the `BuildingStore`) / `BuildSys` are already in scope there — `AiOpponentSystem` at [11]/`:131` already receives them, the precedent). `SystemOrderTest` pins the 11-system order, not ctor args — unaffected.
  - [ ] Factor the canonical damage math into a shared helper so unit and building damage can never drift: `Fixed FinalDamage(Fixed amount, DamageType type, ArmorType targetArmor, Fixed flatArmor)` = `Fixed.Max(Fixed.Zero, amount * table.Get(type, targetArmor) - flatArmor)` (extract from `DamageResolver.cs:62-66`; entity path passes `EffectiveArmor[t]`, building path passes `Fixed.Zero`). Do **not** try to reuse `DamageResolver.Apply` for buildings — it is hard-bound to `EntityWorld` (`world.Health`/`Destroy`).
  - [ ] Add the building-damage path (a `CombatSystem` private method or a `BuildingSystem`/`BuildingStore` method — keep it Godot-free): guard `buildings.Alive[b] && buildings.FactionOf[b] != attackerFaction` (friendly buildings never targeted — AC2), compute `FinalDamage(EffectiveAttackDamage[attacker], DamageTypeOf[attacker], ArmorType.Fortified, Fixed.Zero)`, subtract from `buildings.Health[b]`, and on `≤ 0` call `buildings.Destroy(b)` and push a destroy event (reuse `CombatEventType.MeleeHit`/`RangedHit` for impact; append `BuildingDestroyed` after `AbilityCast` only if a distinct death feedback is wanted — the queue is unfolded so appending is golden-safe). Buildings have no `EffectiveArmor` → flat term is `Zero`. **This one helper is the single building-damage entry point for BOTH the melee instant path (Task 6) and the ranged projectile impact (Task 4b) — one formula, no drift.**

- [ ] **Task 4b — Ranged attackers fire a real projectile at buildings (D-4, sim)** (AC: 2, 2.6)
  - [ ] Add a **building-target discriminator** to `ProjectileStore` (`TargetId` is an **entity** id today, `:21`): a `bool[] TargetIsBuilding` set at spawn, with `TargetId` carrying the building id when `true`. `ProjectileStore` is **not** folded (`SimChecksum.Compute` doesn't take it) → **no fold**, and the flag defaults to `false` (entity-target) so existing projectiles are unchanged.
  - [ ] Thread `BuildingStore?` into `ProjectileSystem` (ctor + construction site `SimulationHost.cs:128`, sibling to the CombatSystem wiring in Task 4). In `ProjectileSystem.ApplyHit` (`:77`), when `TargetIsBuilding`: **bounds-check `b >= 0 && b < _buildings.Count && _buildings.Alive[b]` first** (buildings have no `IsAlive`; a target that died in flight → drop the projectile harmlessly, no throw), then apply damage via the **shared Task-4 helper** (`FinalDamage(Damage, DmgType, ArmorType.Fortified, Fixed.Zero)`) to `buildings.Health[b]`, `Destroy` on `≤0`, push the impact/destroy event. Do **not** run entity splash against a building target. Homing/lifetime reads `_buildings.Position[b]`, not `world.Position`.
  - [ ] In `TickAttackBuildingCombat` (Task 6), the in-range fire step **mirrors `TryDealDamage`'s split** (`CombatSystem.cs:465-504`): `AttackRange[i] > MELEE_THRESHOLD (2.5)` ⇒ `_projectiles.Spawn(...)` with `TargetIsBuilding=true`, the building id, and the `Fortified` armor snapshot; else instant matrix damage via the Task-4 helper. Cooldown gating (`AttackCooldown`/`AttackSpeed`) is identical to the entity path.

- [ ] **Task 5 — `UnitCommand.AttackBuilding = 12` on the wire + the `OrderApplier` case (MP)** (AC: 2, 4)
  - [ ] Append `AttackBuilding = 12` to `UnitCommand` (`EntityWorld.cs:12-29`) — 0–11 frozen. No `ReplayRecorder.VERSION`/`PROTOCOL_VERSION` bump (rides the existing 11-byte `UnitOrder`).
  - [ ] Add the case to `OrderApplier.Apply` (`NetworkCommand.cs`) **inside** the switch **after** the ownership guard (`:135-137`) — the attacker is `o.UnitId` (an entity), so the guard validates own-unit-for-free. **`CommandState[id]` is already set to `AttackBuilding` generically at `:140`** (before the switch), so the case does **not** set it. The case **blind-stores** the target, mirroring the `AttackTarget` case's target-store (`:175-186`) but for a building id: `world.CommandTarget[id] = o.TargetX; world.AttackTarget[id] = -1; world.Flags[id] &= ~EntityFlags.Attacking; ClearPatrolRoute(world, id);`. **Do NOT** copy `AttackTarget`'s `world.AttackTarget[id] = o.TargetX` line — a building id must never enter the entity-space `AttackTarget[]`; set it to `-1`.
  - [ ] **No building validation at apply, and NO `buildings` param.** `AttackBuilding` writes only `EntityWorld`; all validation (bounds `b < Count`, `Alive`, friendly-faction, `Structure`-domain) happens **in-tick** in `TickAttackBuildingCombat` (Task 6), which reverts to Idle on failure — exactly how `AttackTarget` defers validation to `TickAttackTargetCombat`. Consequence: the offline `SelectionSystem` apply site calls `OrderApplier.Apply` **identically to its `AttackTarget` call** (no `buildings` arg), and all three sites (`LockstepManager` `:656-665`, `ReplayPlayer` `:173-181`, offline `SelectionSystem`) stay one switch with **zero new plumbing**. (Contrast `Train`, which needed the `buildings` param because it does its building work *at apply*; `AttackBuilding` does its building work in the tick.)
  - [ ] **Enum-indexed-array check (learned from 2.8's `BuildingType`):** confirm nothing is indexed by `(int)UnitCommand` — there are no parallel arrays keyed on the command enum (verified: only the `CombatSystem.Tick` switch and the gatherer-normalize list, both handled in Task 6). Appending `AttackBuilding=12` therefore adds **no** crash-site.

- [ ] **Task 6 — `TickAttackBuildingCombat` in `CombatSystem` (sim)** (AC: 2, 2.3, 2.4, 2.5)
  - [ ] Add a `case UnitCommand.AttackBuilding:` in the `CombatSystem.Tick` command switch → `TickAttackBuildingCombat(world, i, dt)`, modeled on `TickAttackTargetCombat` (`:228-267`) but reading the **building** store. It inherits the non-combatant/gatherer guards above the switch (`:86-103`).
  - [ ] **Validate FIRST, with a mandatory bounds guard.** `BuildingStore` has **no** `IsAlive` bounds short-circuit (unlike `EntityWorld.IsAlive` at `:672`, which is why the entity `AttackTarget` path is crash-safe on a stale/`-1`/OOB id) — a raw `buildings.Alive[b]` on a bad id throws `IndexOutOfRangeException` and crashes the tick **identically on every peer**. The apply case blind-stores the target (Task 5), so `b` may be `-1`, `≥ Count`, friendly, or a stale entity id. Guard all of it in one shot:
    ```csharp
    int b = world.CommandTarget[i];
    if (_buildings == null || b < 0 || b >= _buildings.Count || !_buildings.Alive[b]
        || _buildings.FactionOf[b] == world.FactionOf[i]
        || (world.AttackDomainOf[i] & AttackDomain.Structure) == 0)
    { world.CommandState[i] = UnitCommand.Idle; world.CommandTarget[i] = -1; return; }
    ```
    This single guard enforces AC2 (friendly never targeted), AC2.4 (`Structure`-domain gate), and crash-safety (bounds), and reverts a rejected/stale/`-1` order cleanly to Idle.
  - [ ] Then: range test `FixedVec3.SqrDistance(world.Position[i], _buildings.Position[b]) <= AttackRange[i]²`; out of range ⇒ set `MoveTarget[i] = _buildings.Position[b]` (chase); in range ⇒ cooldown-gated fire (`AttackCooldown`/`AttackSpeed`) via the **melee/ranged split (Task 4b)** — melee (`AttackRange ≤ 2.5`) resolves instantly via the Task-4 helper, ranged spawns a building-target projectile. On the building's death (`Health ≤ 0 → Destroy`), revert the attacker to Idle.
  - [ ] **Add `AttackBuilding` to the gatherer command-state normalize list at `CombatSystem.cs:88-94`** (which already rewrites `AttackMove/Stop/Hold/AttackTarget/Patrol/Follow/PatrolAppend → Idle` for an active gatherer) so a gathering worker — which defaults to `AttackDomain.All` and is skipped by the `EffectiveAttackDamage==0` guard before the switch, so it never self-reverts — cannot linger in `AttackBuilding`.
  - [ ] Note the building `Position` is a **single Fixed center point** (no sim footprint; `NavObstacleManager.TYPE_SIZE` is presentation-only float). Center-point range is acceptable for 2.9a; a per-type Fixed footprint pad is out of scope (note only).

- [ ] **Task 7 — Presentation: right-click an enemy building issues the order** (AC: 5)
  - [ ] In `SelectionSystem` RMB dispatch (`:289-297`), add a `FindNearestEnemyBuilding(hit, PICK_RADIUS)` picker. **Clone the existing `FindNearestBuilding` (`SelectionSystem.cs:772`, which is faction-agnostic) and add an enemy filter** `if (_buildingStore.FactionOf[b] == localFaction) continue;` — `SelectionSystem` already holds `_buildingStore` (`:63`), so **no new reference** is needed. Insert an `IssueAttackBuildingCommand(bId)` branch **between** the enemy-unit branch and the Move fallback, so dispatch order is **enemy unit → `AttackTarget`; else enemy building → `AttackBuilding`; else Move** (a friendly-building pick must fall through to Move, not swallow it — a dead click).
  - [ ] `IssueAttackBuildingCommand` mirrors `IssueAttackTargetCommand` (`:581-592`): `new UnitOrder(id, UnitCommand.AttackBuilding, Fixed.FromRaw(bId), Fixed.Zero)` then online `EnqueueOrder` / offline `OrderApplier.Apply(_world, in order, _world.FactionOf[id], …)` — **identical to the `AttackTarget` offline call, NO `buildings` arg** (Task 5). Issue only to selected **combat** units (`world.EffectiveAttackDamage[id] > Fixed.Zero`) so workers/non-combatants (which `CombatSystem` skips at `:103`, so they never self-revert) aren't left in a dangling `AttackBuilding` state. Presentation issues an **intent** only; it never writes sim arrays.

- [ ] **Task 8 — Tier-1 tests (xUnit, Godot-free)** (AC: 1, 1.1, 1.2, 2, 2.2, 2.4, 2.6, 3, 4, 6)
  - [ ] **Filter:** a synthetic `attack_domains: ["Air"]` attacker with one air (`CategoryOf.Air`) + one ground enemy in range picks the **air** enemy; a `["Ground"]` attacker picks the **ground** enemy; default-`All` picks nearest regardless of domain. Assert both `FindNearestEnemy` and `FindNearestEnemyGlobal` (chase) honor it (AC1.1). Prove **teeth**: removing the `:139` check makes the AA-unit chase a ground enemy (test goes red).
  - [ ] **Building damage:** an ordered Siege attacker destroys a Fortified building over N ticks; assert `amount × Get(Siege, Fortified)` (1.5×) math and `Destroy` at `Health ≤ 0`; a Pierce attacker's 0.25× barely dents it. **Friendly guard:** an order against an own-faction building no-ops in-tick (no damage). **Domain gate (AC2.4):** an `["Air"]` unit's attack-building order leaves it Idle in-tick (no damage).
  - [ ] **MP parity (AC4):** an `AttackBuilding` `UnitOrder` applied via `OrderApplier.Apply` (offline path, **no `buildings` arg**) then ticked through `CombatSystem` produces byte-identical building `Health` across two runs; the attacker-ownership guard (`:135-137`) rejects a wrong-faction attacker (nothing stored).
  - [ ] **Reject / crash-safety (the panel's critical catch):** an order whose building id is **friendly**, **`-1`**, or **out of `[0,Count)`**, **and** an `["Air"]` (non-`Structure`) attacker's order — each, after `OrderApplier.Apply` **and** one `CombatSystem.Tick`, must **not throw** and must leave the attacker `Idle` with `CommandTarget == -1` (proves the in-tick bounds+friendly+domain guard reverts cleanly; buildings have no `IsAlive` short-circuit, so the `b < Count` guard is load-bearing). A **worker** issued an `AttackBuilding` order is not frozen (gatherer-normalize + picker `EffectiveAttackDamage>0` filter).
  - [ ] **Ranged-vs-building (AC2.6/D-4):** a ranged attacker (`AttackRange > 2.5`) ordered onto a building **spawns a projectile** that, after N ticks of flight, applies `Fortified` matrix damage to `buildings.Health[b]`; a melee attacker (`≤ 2.5`) applies it instantly. A building that dies **while a shell is in flight** is dropped without throwing (the `ProjectileSystem` bounds+`Alive` guard). Two runs byte-identical.
  - [ ] **Ability domain filter (AC6/D-1):** a `SearchArea` effect with `TargetFilter.Air` set touches only `CategoryOf.Air` candidates; `Ground` spares air; **domain=0 touches all** (byte-identical to today). An authored ability carrying the bits **deserializes** (no `JsonException` from the lifted reject). Teeth: removing the `TargetMatcher` domain check makes a `Ground`-only nuke hit a flyer (test goes red).
  - [ ] **Determinism (AC3):** run the golden gate — all **10** byte-identical; `AlgoVersion == 8`; pin `0x983D39AE` unchanged; `VersionStampConsistencyTests` 8/2/1/2 passes; `ApplyUnitDefinitionGuardTest` passes with the new field. Add the **new** `combat-air-ground`, `anti-building`, and `ability-domain-filter` goldens (Task 9) — never edit the existing 10.
  - [ ] Extend the guard-test assertions for `AttackDomainOf` (Task 2).

- [ ] **Task 9 — New golden scenarios (do NOT edit the existing 10)** (AC: 3.4)
  - [ ] Add a `combat-air-ground` golden: a small deterministic scene with an `["Air"]`-restricted unit + mixed air/ground enemies, proving the filter's outcome is stable across two runs (cross-platform-safe — no float, no AI). Add an `anti-building` golden: a unit ordered (via an applied `AttackBuilding` order) to destroy an enemy building — include a **ranged** attacker so the golden exercises the projectile path (run enough ticks for the shell to land), proving the destruction sequence is byte-identical across two runs. Add an `ability-domain-filter` golden: a `SearchArea` ability with a domain bit set, proving deterministic filtered targeting across two runs. Register all three in the golden harness with the existing pattern; they are **new** baselines, not re-records.

- [ ] **Task 10 — In-engine verification (`/godot-verify`)** (AC: 1, 2, 5)
  - [ ] Right-click an enemy building with selected combat units → they path to it, deal matrix damage, and **destroy** it (building mesh + nav obstacle removed, no `IndexOutOfRangeException`). Right-click a **friendly** building → no attack (Move/no-op). Field a temporary `["Air"]` unit vs a mixed air/ground skirmish → it engages only the flier and ignores ground (revert any scaffolding after). Confirm a Siege unit razes a structure far faster than a Pierce unit (matrix). Capture screenshots + read runtime state; revert all verification scaffolding.

- [ ] **Task 11 — Ability-side `SearchArea` domain filter: evaluate the reserved bits (D-1, INCLUDE)** (AC: 6)
  - [ ] **Evaluate the domain bits in `TargetMatcher.Matches` (`TargetMatcher.cs:21`):** after the existing allegiance (`Self/Ally/Enemy/Neutral`) OR-group + `Alive` AND-check, add a domain AND-check — if the filter sets **any** of `Air/Ground/Structure`, the candidate passes only when its `DomainOf(CategoryOf[candidate])` bit is set; if it sets **none**, all domains pass (AC6.1 — preserves today's behavior). Reuse the Task-1 classifier. No float; per-candidate predicate (order-neutral).
  - [ ] **Lift the fail-closed reject** at `EffectNodeJsonConverter.cs:246` (currently throws a located `JsonException` on any `Air/Ground/Structure` bit) so authored `SearchArea` filters carrying domain bits now deserialize.
  - [ ] **Add the 3 bits to the authorable vocabulary** `DraftVocabulary.Filters` (`AbilityDraft.cs:53`) and surface them in the ability-editor filter UI (the 2.5b `[Flags]` filter checkbox surface).
  - [ ] **Update the guarded tests to the new contract:** invert/repurpose `ReservedTargetFilterBit_IsLocatedReject` (the bits are now **accepted + evaluated** — assert acceptance + correct domain filtering, not a reject) and extend `FlagCombinationVocabularyTests` to include the domain bits. **Grep the test project for any other assertion that the reserved bits are rejected / excluded** (`Air|Ground|Structure` near `TargetFilter`/`reserved`/`JsonException`) and update all of them — lifting a fail-closed guard silently breaks every test that pinned it. These are **expected** test changes (NOT part of the determinism fence).
  - [ ] Update the `Effects/TargetFilter.cs` "RESERVED for Story 2.9a" comment to "evaluated since Story 2.9a" and close the `deferred-work.md:128` item.

## Dev Notes

### Current state — precise, source-verified

**Combat target selection (sim).** `CombatSystem.Tick` (`CombatSystem.cs:64`) rebuilds the `SpatialHash`, then scans entities **ascending-id** (`for i in 0..HighWaterMark`), skipping dead / gatherers / zero-`EffectiveAttackDamage` non-combatants (`:86-103`), and dispatches on `world.CommandState[i]` to one of 8 tick handlers. The idle/auto-acquire path `TickIdleCombat` (`:144`) validates the current target, else calls `_spatialHash.FindNearestEnemy` (in-range) then `FindNearestEnemyGlobal` (chase-to-contact). Both acquire methods filter candidates by **faction only** (`SpatialHash.cs:107` and `:139`) then pick strict-nearest (`sqrDist < bestSqrDist`, so ties → lowest-id). `TickStationaryCombat`/`TickAttackMoveCombat`/`TickPatrolCombat` reuse the same acquire calls → **the two `SpatialHash` methods are the single AC1 insertion point.**

**Damage (sim).** `TryDealDamage` (`:465`) gates on `AttackCooldown`, resets to `AttackSpeed`, then splits melee vs ranged at `MELEE_THRESHOLD = 2.5`: ranged spawns a projectile (delayed), melee calls `DamageResolver.Apply` directly, both reading `EffectiveAttackDamage`/`DamageTypeOf` (post-2.2a Effective\*). `DamageResolver.Apply` (`DamageResolver.cs:52`) is **entity-only**: `final = max(0, amount × Table.Get(type, TargetArmor) − world.EffectiveArmor[t])`, then `world.Health[t] -= final`, and on lethal pushes `UnitKilled` + `RecordKill` + `world.Destroy(t)`. It **cannot** damage a building (no `BuildingStore` awareness) — AC2 needs a parallel path sharing only the matrix formula.

**Buildings (sim).** `BuildingStore` is 14 parallel SoA arrays (`MAX_BUILDINGS = 64`), dense `0..Count-1`, **no free-list reuse** in Phase 1. It has `Alive`/`Position` (Fixed center)/`FactionOf`/`Type`/`Health`/`MaxHealth`/construction/production — **no `ArmorType`** and no `DamageType`. `Create(position, faction, type)` hardcodes HP by `BuildingType` and **ignores the JSON** (`hp`/`armor_type` are vestigial). Buildings take **zero** combat damage today; `Destroy(id)` (just `Alive[id]=false`) is called only by the editor delete tool. Buildings are **not** in the combat `SpatialHash` (entity-only buffers) — they register only with `NavObstacleManager` (presentation nav). `ArmorType.Fortified = 4` (enum comment "// Buildings") already has a fully-tuned matrix column (Normal 0.35, Pierce 0.25, Siege 1.50, Magic 0.50, Hero 1.0) — **reuse it, invent no new armor**.

**The air tag already exists.** `EntityWorld.CategoryOf` (`:246`, `UnitCategory` with `Air=4`, `Structure=5`) is the per-unit movement-domain tag, written by the single mapper `ApplyUnitDefinition` from `def.ParsedCategory`, defaulted `Melee` in `Create()`, and **not folded** (presentation-read like `MeshType`). `grep flying|IsAir|CanAttackAir|antiAir` returns **zero** — there is no other air representation, and `MovementSystem` has **no** air special-casing (air units are ground units carrying a tag). So AC1 needs exactly **one** new thing: the attacker's **capability** (`AttackDomainOf`), read against each candidate's `CategoryOf`.

**Command wire (MP).** `UnitOrder` is 11 bytes: `UnitId(ushort)` + `Command(byte)` + `TargetX(int)` + `TargetZ(int)`; `TargetX/Z` are raw `Fixed` ints. `OrderApplier.Apply` (`NetworkCommand.cs:116`) is the **single** command→world switch called verbatim by `LockstepManager`, `ReplayPlayer`, and offline `SelectionSystem` (AR-17 structural parity). **`OrderApplier` sets `CommandState[id] = o.Command` generically at `:140`** (after the ownership guard, before the switch) — per-case bodies do **not** re-set it. `Train=11` is the "UnitId names a building" precedent — handled **before** the ownership guard (it does its building work *at apply*, hence its `buildings` param). `AttackTarget=6` writes `CommandTarget[id] = o.TargetX` (an **entity** id) at apply **without validating**, and defers all target validation to `TickAttackTargetCombat`, which is crash-safe only because `world.IsAlive` (`:672`) short-circuits negative/OOB ids. `AttackBuilding=12` follows this **blind-store-then-validate-in-tick** model (attacker in `UnitId`, so it goes *after* the guard; needs no `buildings` param) — **but `BuildingStore` has no `IsAlive` guard**, so `TickAttackBuildingCombat` must bounds-check the building id first (a building id must never reach `world.IsAlive`/`FactionOf`, and a stale/`-1` `CommandTarget` must not index `buildings.Alive[b]`).

### Determinism notes (no fold, `AlgoVersion` stays 8, 10 goldens byte-identical)

- **`AttackDomainOf` is NOT folded.** It is authored-immutable and read in-sim by target selection — exactly like `DamageTypeOf`, `ArmorTypeOf`, and `CategoryOf`, **all** of which are combat-read authored fields and **none** of which are folded (`SimChecksum` folds `Effective*` outcomes, not authored inputs). A divergent authored domain choice changes which target is hit → the affected entity's already-folded `Position`/`Health` move → caught transitively. This follows [[chimera-checksum-fold-timing-rule]] (fold only when a field first becomes **mutable** mid-match). *(Noted tension: `CollisionRadius`/`SeparationPriorityOf` were folded at v5 despite being authored-immutable, on a "read every tick" rationale. The dominant and most-analogous precedent — `DamageTypeOf`/`ArmorTypeOf`/`CategoryOf`, the sibling combat-targeting authored fields — is **not** folded; follow them, keep v8, ratified in D-3/AC3.)*
- **Anti-building adds no fold.** `buildings.Health`/`Alive`/`ConstructionTimer` are already folded (`SimChecksum.cs:185-188`) — a peer divergence in building damage moves the **directly**-hashed `buildings.Health`. The persistent building target rides the already-folded `CommandTarget` under `CommandState = AttackBuilding`; **no new folded SoA field** is introduced.
- **`CanonicalModelHash` (AlgoVersion 2) is untouched** — it folds `ScenarioUnit` **placement** fields, not `UnitDefinition` stats. Keep `attack_domains` a `UnitDefinition` property (resolved via `ApplyUnitDefinition`); **do not** add it to `ScenarioUnit` (that would move the start-state hash).
- **Goldens stay byte-identical (verified):** no golden spawns an Air-category unit (only `"Worker"`), so default-`All` filtering is inert; the 3 building goldens never issue an attack-building order, and explicit-order-only never auto-targets buildings. Empirically re-run all 10 after wiring — a moved golden means a leaked behavior/fold change; **fix it, don't re-baseline.** Add coverage as the **new** `combat-air-ground` + `anti-building` + `ability-domain-filter` goldens.
- **Ability domain filter (AC6/D-1) moves no golden, adds no fold.** `TargetMatcher` reads authored `CategoryOf`; "**no domain bit = all domains**" makes the new check a no-op for every existing `SearchArea` (all carry domain=0). Effect mutations already fold via `Health`/modifiers. The `ability-cast`/`modifier`/`passive` goldens stay byte-identical; the new `ability-domain-filter` golden covers the feature. The lifted converter reject + `DraftVocabulary` + inverted `ReservedTargetFilterBit` test are **expected** changes (authoring path, not the determinism fence).
- **Ranged-vs-building projectiles (AC2.6/D-4) move no golden, add no fold.** `ProjectileStore` is **not** a `SimChecksum.Compute` input (signature = `EntityWorld`/`BuildingStore`/`ResourceStore`/`FactionRegistry`/`ModifierStore?`), so the new `TargetIsBuilding` flag is fold-neutral; it defaults to entity-target and no existing golden spawns a building-target projectile. The projectile is spawned deterministically inside the ascending-id combat tick; damage lands on the already-folded `buildings.Health`.
- **Fixed / ascending-id everywhere:** the domain check is an integer bit-AND inside the existing ascending-id `SpatialHash` scan; the building search iterates `BuildingStore` ascending-id with `FixedVec3.SqrDistance`; the matrix is integer-indexed (`ArmorType.Fortified` is a compile-time constant, cross-platform safe). No `float`/`Mathf`/`System.Random`/`Dictionary` enumeration enters the tick.

### The determinism fence (git status must NOT touch — a change here means a fold/behavior leak slipped in)

`godot/src/Core/SimChecksum.cs` (AlgoVersion 8 + folded set/order) · `godot/ProjectChimera.Sim.Tests/Golden/SimChecksumCoverageGuardTest.cs` (`ExpectedV8Hash = 0x983D39AE`) · `godot/ProjectChimera.Sim.Tests/Meta/VersionStampConsistencyTests.cs` (8/2/1/2 + "0.1") · `godot/src/Core/Definitions/CanonicalModelHash.cs` (AlgoVersion 2) · and all **10** `godot/ProjectChimera.Sim.Tests/Golden/*.golden.txt` (ability-cast, ai-active, command-vocabulary, formation-separation, golden-applier, golden-multifaction, golden-scenario, modifier, passive, same-tick-tie-break). Adding the two **new** goldens is expected; touching any existing one is not.

### Reuse — do NOT reinvent

- **`CategoryOf`** already identifies fliers (`== Air`) — no new target-side data. Add only the attacker-capability field, the same way `CategoryOf`/`CollisionRadius`/`SeparationPriorityOf` were added in 1.13 (one `ApplyUnitDefinition` write, one `Create` default, two guard assertions).
- **`ArmorType.Fortified` + the `DamageTable` matrix** — pass `Fortified` and call `DamageTable.Get(type, Fortified)` (the same `_table` instance `CombatSystem` already holds). No new balance storage.
- **The damage formula** (`max(0, amount × matrix − armor)`, `DamageResolver.cs:62-66`) — extract the multiplier/floor math into a shared helper so unit and building damage are bit-identical (buildings pass `flatArmor = Zero`).
- **The `Train`/`CastAbility` dedicated-command shape + the `AttackTarget` blind-store-then-validate-in-tick model** — `AttackBuilding` is a hybrid: it names a building (like `Train`) but validates in-tick (like `AttackTarget`), so — unlike `Train` — it needs **no** `buildings` param on `OrderApplier`. `CombatSystem` gets `BuildingStore` threaded directly (Task 4); the in-tick friendly/bounds guard mirrors `BuildingSystem.TrainUnitCommand`'s `bounds + Alive + FactionOf != faction` shape.
- **`IssueAttackTargetCommand` + the RMB dispatch** (`SelectionSystem.cs:289-297, 581-592`) — clone `IssueAttackTargetCommand`→`IssueAttackBuildingCommand`, and clone the faction-agnostic `FindNearestBuilding` (`:772`) + add an enemy filter for `FindNearestEnemyBuilding`; insert one middle branch (enemy-unit → enemy-building → Move).
- **`CommandTarget` (folded) shared under `CommandState`** — `AttackTarget`/`Follow` already store different id kinds in `CommandTarget` disambiguated by `CommandState`; `AttackBuilding` adds a building-id kind the same way (no new SoA, no fold).

### Regression risks (must not break)

- **Default `AttackDomain.All`** must be byte-identical to today for every existing unit (no roster edits; the AC1 restriction is a synthetic test unit). If any of the 10 goldens move, a non-default default or a roster edit leaked in.
- **`AttackBuilding` case placement:** it names an **entity** attacker (`UnitId`) → it must go **after** the ownership guard (unlike `Train`). Putting it before would skip the own-unit anti-cheat check.
- **Never let a building id reach `world.IsAlive`/`world.FactionOf`** (entity space). Keep the building id only in `CommandTarget` under `CommandState == AttackBuilding`, validated against `BuildingStore`. In the apply case set `AttackTarget[id] = -1` — **do not** copy `AttackTarget`'s `AttackTarget[id]=o.TargetX` line (that puts a building id in entity space).
- **The apply case blind-stores; the tick MUST bounds-check.** `OrderApplier` sets `CommandState` at `:140` before the switch, and the `AttackBuilding` case (like `AttackTarget`) stores the target without validating — so `TickAttackBuildingCombat` **must** guard `b >= 0 && b < _buildings.Count` **before** indexing `_buildings.Alive[b]` (buildings have no `IsAlive` short-circuit; a stale/`-1`/`≥Count` id crashes the tick otherwise, deterministically on all peers). Revert a failed order to Idle.
- **`AttackBuilding` needs no `buildings` param** — it writes only `EntityWorld`; the offline `SelectionSystem` call matches its `AttackTarget` call. Do **not** thread a `BuildingSystem` into `SelectionSystem` (it holds only a `BuildingStore`, and none is needed — the tick does the building work).
- **Don't leave a non-combatant stuck in `AttackBuilding`** — `CombatSystem` skips `EffectiveAttackDamage==0` units before the switch, so they never self-revert; the picker issues only to combat units and `AttackBuilding` is added to the gatherer-normalize list (Task 6).
- **`DamageResolver.Apply` cannot be pointed at a building** — do not try; share only the formula helper, or `world.Destroy(t)` corrupts the wrong store.
- **Append `AttackBuilding = 12`; never renumber** 0–11 (replay back-compat). No `ReplayRecorder.VERSION`/`PROTOCOL_VERSION` bump.
- **`SystemOrderTest`** pins the 11-system registration order — adding a ctor arg to `CombatSystem` is fine; changing its index is not.
- **Now IN scope (per Alec, 2026-07-01):** the ability-side `SearchArea` domain filter (D-1 → AC6/Task 11) and the ranged-vs-building **projectile** (D-4 → AC2.6/Task 4b).
- **Out of scope (note, don't build):** per-building `ArmorType` SoA (D-3, `Fortified` constant only); building footprint in sim (center-point range); `StatusFlags` (Disarmed/etc.) combat honoring (`deferred-work.md:170` — adjacent, **not** in 2.9a's ACs); auto-acquire of buildings (`deferred-work.md:13` — later); a fielded AA unit in the shipping rosters (content decision, kept off every golden). The AI does not issue attack-building orders (in-tick, unchanged).

### Testing standards

- **Tier-1** = xUnit, Godot-free (`ProjectChimera.Sim.Tests`) — the filter/building-damage/guard/parity/determinism tests in Tasks 8–9. All sim logic testable without Godot.
- **Tier-2 / in-engine** = `/godot-verify` (Task 10) for the right-click picker + chase + destruction + friendly no-op.
- **Prove each new gate has teeth** (Epic-1 retro action): the filter test must go **red** if the `:139` (chase) check is removed; the friendly-building guard test must go red if the `FactionOf` check is removed; the domain-gate (AC2.4) test must go red if the `Structure` gate is removed.

### Project Structure Notes

- **Sim (Godot-free, `Fixed`/int, ascending-id):** `godot/src/Core/AttackDomain.cs` (new enum + shared classifier), `godot/src/Core/Definitions/UnitDefinition.cs`, `godot/src/Core/EntityWorld.cs`, `godot/src/Navigation/SpatialHash.cs`, `godot/src/Combat/CombatSystem.cs`, `godot/src/Combat/DamageResolver.cs` (extract shared formula helper), `godot/src/Combat/ProjectileStore.cs` + `godot/src/Combat/ProjectileSystem.cs` (building-target projectile, Task 4b), `godot/src/Core/BuildingStore.cs` and/or `godot/src/Economy/BuildingSystem.cs` (building-damage method), `godot/src/Multiplayer/NetworkCommand.cs` (`OrderApplier` case), `godot/src/Core/Sim/SimulationHost.cs` (thread `Buildings` into **both** `CombatSystem` and `ProjectileSystem`). Ability-side domain filter (Task 11): `godot/src/Effects/TargetMatcher.cs` (evaluate the bits), `godot/src/Core/Definitions/EffectNodeJsonConverter.cs` (lift the reject), `godot/src/Core/Definitions/AbilityDraft.cs` (`DraftVocabulary.Filters`), `godot/src/Effects/TargetFilter.cs` (comment).
- **Presentation (`using Godot`, Tier-1 can't cover → Task 10):** `godot/src/UI/SelectionSystem.cs` (enemy-building picker via the existing `_buildingStore` + `IssueAttackBuildingCommand`); the ability-editor filter surface (the 2.5b `[Flags]` filter UI) gains the 3 domain bits (Task 11). `LockstepManager.cs`/`ReplayPlayer.cs` need **no change** — `AttackBuilding` carries no `buildings` param.
- **Tests:** `godot/ProjectChimera.Sim.Tests/…` (new combat/filter/anti-building tests + 2 new goldens). Matches the established folder map; the one new sim file (`AttackDomain.cs`) sits beside `UnitCategory.cs` — **add it to `SimSources.props`** if the analyzer/Tier-1/boundary test is to see it (the 2.1 `src/Effects` lesson).

### Project Context Rules (from `_bmad-output/project-context.md`)

- **Sim/Presentation boundary is sacred:** `CombatSystem`/`SpatialHash`/`BuildingStore`/`EntityWorld`/`DamageResolver`/`NetworkCommand` are sim — no `using Godot`, no `float` gameplay state, no `Vector3`. `SelectionSystem` is presentation — it **reads** sim arrays and **issues intents**, never mutates sim SoA directly.
- **Determinism:** all gameplay math in `Fixed`; ascending-id iteration; no `float`/`Mathf`/`System.Random`/wall-clock/`Dictionary` enumeration in the tick. `attack_domains` string→flags parsing is load-time only (like `ParsedCategory`); the in-tick check is an integer bit-AND.
- **Single def→SoA mapper (A2):** `AttackDomainOf` flows through `EntityWorld.ApplyUnitDefinition` only — never hand-copied in a spawn path; defaulted in `Create()`; guarded by `ApplyUnitDefinitionGuardTest`. ([[chimera-content-validator-bound-behavioral-params]] discipline; the 1.12/1.13 zombie-slot lesson.)
- **Data-driven:** the air/ground capability is authored as one JSON field (`attack_domains`) a creator edits; building armor stays data-adjacent (`Fortified` matches 100% of current data; a per-building armor SoA is the noted future upgrade).
- **Godot C# gotchas:** classes inheriting Godot types must be `partial`; `[Export]` floats are presentation-only; `GD.Print()` not `Console.WriteLine()`; `#nullable enable` per file. The `.sln` is `godot.sln`.

### References

- Story spec + ACs: `_bmad-output/planning-artifacts/epics.md#Story-2.9a` (L1000-1014); Epic-2 sequencing note L840; downstream deps L1044 (2.10), L1026 (2.9b).
- FR-11 / FR-12 / AR-8 / NFR-4: `epics.md` FR table + AR-8 (effect/combat surface). Design intent + "needs-code" flags: `_bmad-output/fma-faction-design.md:112,124,171,173` (anti-air discrimination, anti-structure siege role, Pierce=anti-Light-not-anti-tank). Shared-TargetFilter vision: `_bmad-output/game-architecture.md:415`. Buildings-are-a-separate-id-space: `_bmad-output/architecture.md:67`.
- Determinism precedent: `[[chimera-checksum-fold-timing-rule]]`, `[[chimera-enum-indexed-array-touch-sites]]` (BuildingType consumers — relevant if a per-building armor SoA is ever added), `[[godot-mcp-ui-verify-via-signals]]` (Task 10 method).
- deferred-work: `_bmad-output/implementation-artifacts/deferred-work.md:128` (Air/Ground/Structure TargetFilter — the D-1 ability-side item), `:170` (StatusFlags combat honoring — out of scope), `:13` (AttackMove + auto-acquire buildings — later).
- Reuse templates: `2-8-*.md` (`OrderApplier` `BuildingSystem?` threading + `Train=11` dedicated-command precedent + building-ownership guard + the enum-touch-site discipline), `1-12-*.md` (`AttackTarget`/`OrderApplier` unification, replay-vs-live parity, the faction-guard lesson), `1-13-*.md` (`CategoryOf` + single-mapper + guard-test triad).
- Source (verified this session, ultracode recon): `CombatSystem.cs:50,64,86-103,144,228-267,465-504` · `SpatialHash.cs:74,103-115(107),127-150(139)` · `DamageResolver.cs:22,52-76` · `DamageTable.cs:29-38(35 Fortified),55` · `BuildingStore.cs:28-33,83,88,150` · `BuildingSystem.cs:372` · `EntityWorld.cs:12-29(AttackBuilding=12 next),246(CategoryOf),293(CommandTarget folded v4),479/521-530(Create),582/605(ApplyUnitDefinition)` · `UnitCategory.cs:14` · `UnitDefinition.cs:14-147,117-127,232,255-263` · `Effects/TargetFilter.cs:16-40` · `TargetMatcher.cs:21` · `EffectNodeJsonConverter.cs:246` · `AbilityDraft.cs:53` · `SimChecksum.cs:77,94-178,125-132,183-188` · `SimChecksumCoverageGuardTest.cs:120` · `VersionStampConsistencyTests.cs:51-63` · `CanonicalModelHash.cs:29,84-92` · `NetworkCommand.cs:72-93,116,122-137,175-186` · `LockstepManager.cs:656-665` · `ReplayPlayer.cs:173-181` · `SelectionSystem.cs:289-297,581-592` · `SimulationHost.cs:126,131` · `ApplyUnitDefinitionGuardTest.cs:44-127` · `ProjectileStore.cs:21` · `ProjectileSystem.cs:77` · `CombatEventQueue.cs:8-18` · goldens `ProjectChimera.Sim.Tests/Golden/*.golden.txt`.

## Dev Agent Record

### Agent Model Used

### Debug Log References

### Completion Notes List

### File List
