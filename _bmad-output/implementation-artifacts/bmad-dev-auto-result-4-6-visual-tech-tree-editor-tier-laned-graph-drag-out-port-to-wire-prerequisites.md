---
status: blocked
---

# BMad Dev Auto Result

Status: blocked
Blocking condition: missing previous-story continuity — story 4.6 requires story 4.5's implementation (`epic-4-context.md` Cross-Story Dependencies: "4.6 needs 4.2 + 4.5"; 4.6's UX spec explicitly builds the tech-tree node inspector on "the same right-dock inspector as the building editor"), but 4.5's actual code is **absent from `master`** despite bookkeeping marking it done.

Evidence, in order of discovery:

1. This run started with a clean tree (a stray uncommitted frontmatter edit on this very file, `status: ready-for-dev` vs. body `Status: blocked`, was discarded via `git checkout --` to restore the last consistent committed state — the same self-inflicted dirty-tree loop this file has hit twice before).
2. `_bmad-output/implementation-artifacts/sprint-status.yaml:113` reads `4-5-in-app-building-definition-editor-unit-card-pattern-right-dock-inspector: done`, and `deferred-work.md` carries DW-64 through DW-72 from "code review of story-4.5", citing `source_spec: spec-4-5-in-app-building-definition-editor-unit-card-pattern-right-dock-inspector.md` and locations in `godot/src/CreationSuite/BuildingCardPanel.Edit.cs`.
3. **None of those files exist in `master`.** `godot/src/CreationSuite/BuildingCardPanel.cs`, `BuildingCardPanel.Edit.cs`, `godot/src/Core/Definitions/FactionWriter.cs`, and `spec-4-5-in-app-building-definition-editor-unit-card-pattern-right-dock-inspector.md` were added by commit `1521d73` ("story 4-5-...: implemented and reviewed via bmad-dev-auto"), but `1521d73` is **not an ancestor of `master`'s HEAD (`7f550bd`)**.
4. `git reflog` shows `master` briefly pointed at `1521d73` (`master@{4}`) and, earlier, at `0e5784b` — story 4.4's implementation commit (`master@{9}`) — before both were reset back out of `master`'s history. The discarded commits survive only on rescue refs `refs/heads/attempt-preserve/20260708-213039-0231-1521d739` and `refs/heads/attempt-preserve/20260708-213039-0231-0e5784bc` (plus dirty-worktree snapshots on `refs/attempt-preserve-dirty/...-5ad50979-1` and `...-eb905213-1`), all from the same `20260708-213039-0231` event.
5. Instead of the real code, `master`'s mainline only picked up bookkeeping-only commits — `935d642` ("record DW-63 from story 4.4 review") and `232f44f` ("commit story 4.5 completion bookkeeping") — which mark both stories done in `deferred-work.md`/`sprint-status.yaml` without their source changes ever landing.
6. `.bmad-loop/policy.toml` has `[scm] isolation = "none"`, meaning work is expected to happen in place on the checked-out branch with no branch/worktree indirection — so these `attempt-preserve*` refs are not a designed merge-back path, they look like an emergency snapshot taken before an unexplained reset (likely a stalled/aborted run) that then had its status bookkeeping applied by hand or by a later session without reconciling the code.

Net effect: story 4.6 has no building-editor surface to extend, and stories 4.4/4.5 are falsely reported complete on `master` — a two-story gap, not one.

This needs a human decision before any dev-auto run touches epic 4 again: whether to recover the reviewed work by merging/cherry-picking `attempt-preserve/20260708-213039-0231-1521d739` (story 4.5) and `attempt-preserve/20260708-213039-0231-0e5784bc` (story 4.4) into `master` (resolving DW-64..72 and whatever 4.4 left behind as part of that), or to deliberately redo those stories and correct the `done` bookkeeping if the preserved branches are not trusted. Either way, `sprint-status.yaml` and `deferred-work.md` are currently lying about what's actually in the tree, which will keep misleading future dev-auto/context-compilation runs for epic 4 until it's fixed.

---

**Resolved (bmad-loop-resolve, 2026-07-09):** Human reviewed this finding and directed recovery of both stranded rescue branches into `master` rather than a redo. `attempt-preserve/20260708-213039-0231-0e5784bc` (story 4.4) and `attempt-preserve/20260708-213039-0231-1521d739` (story 4.5) were merged into `master` (merge commits recovering each story's reviewed code — both merges were conflict-free per `git merge-tree` dry runs, and `deferred-work.md`'s DW-64..72 entries de-duplicated automatically since both branches had added identical text). `sprint-status.yaml`'s `4-4` entry was flipped `backlog` → `done` to match; `4-5` was already (truthfully, now) `done`. Story 4.4's orphaned spec file was copied from `.bmad-loop/runs/20260708-213039-0231/deferred/.../spec-4-4-....md` into `_bmad-output/implementation-artifacts/` alongside 4.5's (which the rescue commit already carried). Build and Tier-1 test suite verified green post-merge (one pre-existing, unrelated `ProceduralMapGeneratorTests` golden-hash failure confirmed present on `master` before these merges too — not caused by this recovery). Story 4.6 now has a real `BuildingCardPanel`/right-dock-inspector surface to extend; this run can be re-armed and re-driven.

---

**New blocking finding (bmad-dev-auto re-drive, 2026-07-09):** The code-recovery gap is fixed, but a second, narrower gap surfaced during step-01 context loading: the recovered spec files' own frontmatter `status` disagrees with `sprint-status.yaml`.

- `spec-4-4-data-driven-supply-cap-model-per-scenario.md` frontmatter: `status: 'in-review'`, `review_loop_iteration: 0`, `followup_review_recommended: true`.
- `spec-4-5-in-app-building-definition-editor-unit-card-pattern-right-dock-inspector.md` frontmatter: `status: 'in-review'`, `review_loop_iteration: 0`, `followup_review_recommended: false`, and its own `## Review Triage Log` shows a completed 2026-07-09 review pass (0 `intent_gap`, 0 `bad_spec`, 4 `patch` findings all listed under `addressed_findings`) — i.e. the review ran and its findings were applied in-place, but the run appears to have been cut off (the same reset event documented above) before the frontmatter was flipped to `done`.
- `sprint-status.yaml` lines 112-113 mark both `4-4-...` and `4-5-...` as `done` — per the prior resolution note, this was written to match the recovered code landing on `master`, not derived from the specs' own status fields.
- Neither recovered spec file has any git history on `master` (`git log --follow` on both paths returns nothing) — they were staged directly from the rescue branches/run-dir copy and never went through a commit that would have carried a subsequent "flip to done" edit, so there is no later revision to fall back to.

Per this workflow's own continuity rule (step-01, INSTRUCTIONS 1.A.5): scanning for the most recent same-epic spec with `status: done` and a lower story number than 4.6 finds none — the highest-numbered candidate (4.5) is `in-review`, not `done`. The rule requires halting rather than a dev-auto session silently deciding whether that review was actually complete enough to build on.

This is a judgment call, not a mechanical fix: it's plausible the review genuinely finished clean (the triage log shows all findings addressed, zero deferred-to-follow-up-review flags on 4.5) and only the status-flip commit was lost in the reset — in which case flipping both specs' frontmatter to `done` (and clearing `sprint-status.yaml`'s now-redundant-but-accurate `done` marks) is a rubber stamp. But it's also possible verification (build/tests/`/godot-verify`) never actually ran to completion before the reset, in which case 4.4/4.5 need a real review/verification pass, not just a status edit, before 4.6 extends their surface.

**Blocking condition:** `missing previous-story continuity decision` — needs a human (or `/bmad-loop-resolve`) call on whether to (a) trust the completed-looking 4.5 review triage log and 4.4's own state, flip both specs' frontmatter `status` to `done` (with `sprint-status.yaml` already agreeing), and re-arm this run; or (b) re-drive 4.4 and/or 4.5 through `step-04-review.md` (or a fresh verification pass) before touching 4.6.

---

**Resolved (bmad-loop-resolve, 2026-07-09, second pass):** Before acting on the frontmatter question, this pass re-verified the prior "Resolved" note's own claim by git archaeology, since the escalation context handed to this session carried no recorded escalation detail (empty `escalations: []`, generic `paused_reason`) — and the claim did not hold up.

**The prior recovery had itself been silently reset away.** `git reflog show master` showed: the two rescue-branch merges landing at `19:46` (`d6fab60`, `0fc09ab`) and a `sprint-status.yaml` reconciliation commit at `19:50` (`4d93b4d`) — then, at `20:00`, an untracked `reset: moving to 7f550bdf...` event put `master` right back to the pre-merge commit, discarding both merges from history. Neither `1521d73` (story 4.5) nor `0e5784b` (story 4.4) was an ancestor of `master`'s HEAD at this session's start, and `godot/src/CreationSuite/BuildingCardPanel.cs` (and the rest of both stories' code) was absent from both HEAD and the working tree. This is the same self-inflicted reset pattern the original finding (above) already flagged happening twice before — it had claimed the fix itself as a third/fourth victim. The subsequent "New blocking finding" pass (immediately above) did not catch this: it trusted the first "Resolved" note's claim at face value and only re-checked the specs' frontmatter, not whether the merge commits were still reachable from HEAD.

**Recovery path used:** the orphaned commit `4d93b4d` (the reconciliation commit, tip of surviving rescue ref `attempt-preserve/20260708-213039-0231-4d93b4d7`) still contained the fully-merged, fully-reconciled state — both stories' code/tests and the already-correct `sprint-status.yaml`. Verified `git merge-base --is-ancestor HEAD attempt-preserve/20260708-213039-0231-4d93b4d7` (clean fast-forward candidate) and that a diff between it and the then-current HEAD+working-tree showed zero drift beyond the missing code (no reconciliation needed). Ran `git merge --ff-only attempt-preserve/20260708-213039-0231-4d93b4d7` — fast-forwarded cleanly, `master` HEAD is now `4d93b4d`. Re-verified post-merge: `BuildingCardPanel.cs`/`.Edit.cs`, `SupplyConfig.cs`, `FactionWriter`'s building-sync additions, and all new Tier-1 tests are present in the working tree; `sprint-status.yaml` already read `4-4: done` / `4-5: done` from the recovered commit.

**Frontmatter decision:** Human directed flipping **both** `spec-4-4-data-driven-supply-cap-model-per-scenario.md` and `spec-4-5-...md` frontmatter `status` to `done` now, given code recovery, rather than gating 4.4 on an independent follow-up review pass first. This is a deliberate, informed override of `spec-4-4`'s own `followup_review_recommended: true` flag (which was raised because two review passes' fixes concentrated in the multiplayer-determinism/lobby-handshake hash subsystem) and departs from this repo's own precedent for that flag (`spec-3-15`, `spec-3-16` both got an actual second independent review pass before flipping to `done`). Recorded here so the exception is visible to future sessions, not silently lost. Both specs' frontmatter `status` fields have been set to `done` accordingly; `sprint-status.yaml` already agreed. Story 4.6 now has a real, code-verified `BuildingCardPanel`/right-dock-inspector surface, on an ancestor-verified `master`, to extend.

**Residual risk carried forward, not resolved here:** the root cause of the recurring silent `master` reset (now confirmed to have destroyed recovered work at least once, likely twice) has not been diagnosed or fixed — only worked around by re-recovering from a surviving rescue ref. If it recurs, a future session may find `master` missing this recovery too. Worth a dedicated investigation into what step/hook is issuing these resets before running further dev-auto epic-4 work.
