{
  "name": "chimera-nakama-modules",
  "version": "1.0.0",
  "description": "Project Chimera server-validated hero-profile Nakama runtime module (Story 9.12): a validating write RPC + an attestation RPC over an Owner-Read / No-Client-Write storage object.",
  "private": true,
  "type": "module",
  "main": "build/index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --format=cjs --target=es2017 --outfile=build/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
