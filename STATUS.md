> ⚠️ DEPRECATED 2026-04-16 — Remaining items migrated to Snapshot.md. Full history archived at 30_Archive/Chimera_STATUS_archived_2026-04-16.md. Do not update this file.

# Project Chimera — GDD Implementation Status

## How to Use This File
- ✅ = Complete and tested
- 🔨 = Currently in progress
- 📋 = Specified in GDD, not started
- ❓ = Needs design decision before implementing
- 🚫 = Deferred / cut from current phase

## Legend
Each item references the GDD section it comes from.

---

## Phase 0 — Technical Prototypes (Target: Months 1-3)

### P0.1 — Unit Count Stress Test
| Item | Status | GDD Section | Notes |
|------|--------|-------------|-------|
| Project scaffolding (folders, namespaces) | ✅ | §2 | src/ folder structure with namespaces |
| FixedPoint math struct | ✅ | §2, §6 | Custom 16.16 Fixed + FixedVec3 |
| Entity storage (SoA arrays) | ✅ | §2 | EntityWorld with SoA, free list, 4096 max |
| SimulationLoop (30 tick/sec) | ✅ | §2 | Fixed timestep accumulator, ISimSystem |
| MultiMesh rendering bridge | ✅ | §2 | Interpolated rendering from sim arrays |
| Basic movement (steering) | ✅ | §3 | MovementSystem: seek-to-target |
| Basic combat (attack + damage) | ✅ | §3 | CombatSystem: targeting, damage matrix, cooldowns, death |
| Performance profiling at 500 units | ✅ | §10 | 300-308 FPS with full combat (O(n²) targeting) |
| Performance profiling at 1000 units | ✅ | §10 | ~350 FPS with spatial hash (was 50 FPS O(n²)) — 7× improvement |

### P0.2 — Edit-Play Loop
| Item | Status | GDD Section | Notes |
|------|--------|-------------|-------|
| Terrain placeholder (flat plane) | ✅ | §5 | 256×256 PlaneMesh with GLSL grid shader (1-unit cells) |
| Entity placement (click-to-spawn) | ✅ | §5 | Raycast to Y=0 plane; Tab cycles faction; EntityPlacer.cs |
| Play/Edit mode toggle | ✅ | §5 | GameState singleton; F5 toggles; sim only runs in Play mode |
| Camera controller (RTS camera) | ✅ | §3 | WASD pan, scroll zoom, middle-mouse rotate+tilt; RtsCameraController.cs |
| Entity selection (stretch) | ✅ | §3 | Click picks nearest unit in Play mode; yellow ring + HP bar overlay |

### P0.3 — AI Art Pipeline Evaluation
| Item | Status | GDD Section | Notes |
|------|--------|-------------|-------|
| Generate test faction (8 units) | 📋 | §8 | Hunyuan3D or Tripo — external tool work |
| Generate test buildings (4) | 📋 | §8 | External tool work |
| Style consistency assessment | 📋 | §8 | After assets land |
| Import pipeline to Godot | ✅ | §8 | MeshLoader (GLB→Mesh, box fallback), FactionDefinition+UnitDefinition JSON schema, AssetPreviewScene (turntable + arrow-key cycling), MainScene loads from alpha_faction.json |

---

## Phase 1 — Core RTS Engine (Target: Months 3-9)

### §3 Resource System
| Item | Status | GDD Section | Notes |
|------|--------|-------------|-------|
| ResourceDefinition data class | ✅ | §3 | ResourceNodeStore + ResourceStore (pure C#, SoA) |
| Resource node (map object) | ✅ | §3 | ResourceNodeStore (64 nodes), ResourceNodeBridge (glowing sphere visual, depletes visually) |
| Gatherer behavior (worker AI) | ✅ | §3 | GatheringSystem: Idle→MovingToResource→Gathering→MovingToBase→deposit→repeat |
| Resource UI (HUD display) | ✅ | §3 | Ore counters per faction + active node count in HUD |
| Supply cap system | ✅ | §3 | ResourceStore: SupplyUsed/SupplyCap arrays; SupplySystem ISimSystem recalculates per tick; EntityPlacer enforces cap before spawning combat units |

### §3 Combat System
| Item | Status | GDD Section | Notes |
|------|--------|-------------|-------|
| DamageType/ArmorType matrix | ✅ | §3 | 4×5 multiplier table, static lookup, data-ready for JSON Phase 1 |
| Attack cooldown system | ✅ | §3 | Per-entity Fixed cooldown ticked in CombatSystem |
| Projectile system | ✅ | §3 | ProjectileStore SoA (512 slots, free list) + ProjectileSystem ISimSystem (tracking, hit detection) + ProjectileBridge MultiMesh (yellow sphere, Y+0.8 offset). Ranged units (AttackRange > 2.5u) spawn projectiles; melee uses instant damage. |
| Splash/AoE damage | ✅ | §3 | `UnitDefinition.SplashRadius` (JSON `splash_radius`). `EntityWorld.SplashRadius[]` SoA. `ProjectileStore.SplashRadius[]` copied at fire time. `ProjectileSystem.ApplySplash()`: O(n) scan on hit, full-damage to all enemies in radius (excludes primary target and friendlies). Siege Engine has `splash_radius: 3.0` in JSON. 0 errors. |
| Combat feedback (particles, sound) | ✅ | §3 | `CombatEventQueue` (pure C# ring buffer, 256 slots). `CombatFeedbackBridge` (48-slot pooled `MeshInstance3D` flashes: orange melee, yellow ranged, red splash, white kill). Camera shake via `RtsCameraController.SetShake()` on kill events. 0 errors, 0 warnings. |
| Unit death and cleanup | ✅ | §3 | Destroy() returns ID to free list; bridges hide dead slots |

### §3 Unit Framework
| Item | Status | GDD Section | Notes |
|------|--------|-------------|-------|
| UnitDefinition data class | ✅ | §3 | JSON-driven; spawning wired to alpha_faction.json (BuildingSystem + EntityPlacer + MainScene.SpawnWorker) |
| 6 base archetypes implemented | ✅ | §3 | Worker, Melee, Ranged, Siege, Air, Structure. `train_time`+`vision_range` in UnitDef+JSON. BuildingSystem.TrainUnit() maps building type→category (Barracks→Melee, ArcheryRange→Ranged, SiegeWorkshop→Siege). EntityPlacer U-key cycles 7 combat archetypes. CommandCard shows correct unit per building. |
| Command system (Move, Attack-Move, etc.) | 🟡 | §3 | PARTIAL (readiness 2026-06-21): Move/Stop/Attack-Move built (UnitCommand enum; CombatSystem branches; R-Click=Move, A+Click=AttackMove, S=Stop, H=Hold). MISSING: single-target Attack (right-click enemy), Patrol, Follow; Hold currently aliases Stop. → Story 1.12 / DG-1. |
| Selection system | ✅ | §3 | Click select, box-select, move command, control groups (Ctrl+1-9 assign, 1-9 recall); P1-only faction filter on click + box-select |
| Formation movement | 🟡 | §3 | PARTIAL (readiness 2026-06-21): grid scatter only (sqrt(N) columns, 2u spacing) + symmetric separation. MISSING: role-based formations (front/back by archetype) and priority yielding (moving>idle), plus per-unit collision-radius/push-yield fields. → Story 1.13 / DG-2. |

### §3 Fog of War
| Item | Status | GDD Section | Notes |
|------|--------|-------------|-------|
| Vision grid (render texture) | ✅ | §3 | FogOfWarSystem: 128×128 byte grid, 2u/cell, covers ±128 world units |
| Per-unit vision stamping | ✅ | §3 | Circle stamp per P1 unit each tick using VisionRange (default 8u) |
| Fog shader overlay | ✅ | §3 | FogOfWarBridge: R8 ImageTexture → blend_mix spatial shader on Y=1.5 plane |
| Explored vs unexplored state | ✅ | §3 | Three states: Unexplored (black), Explored (55% tint), Visible (transparent) |

### §3 Pathfinding
| Item | Status | GDD Section | Notes |
|------|--------|-------------|-------|
| NavigationServer3D direct queries | ✅ | §2 | PathRequestSystem: 30 queries/frame rate limit, per-entity waypoint queue, formation offset on multi-unit move, direct-steer fallback |
| Spatial hash grid (collision) | ✅ | §2 | 32×32 fixed grid, counting sort rebuild, O(k) queries |
| Steering behaviors | ✅ | §3 | Separation (SpatialHash), seek, arrive (SLOW_RADIUS=4) |
| NavMesh obstacles for buildings | ✅ | §3 | `NavObstacleManager` (src/UI/): per-building `StaticBody3D` children on `NavigationRegion3D`; `BakeNavigationMesh(false)` on any add/remove. Navmesh switched to geometry-baked (`GeometryParsedGeometryType=StaticColliders`, `GeometrySourceGeometryMode=RootNodeChildren`, `CellSize=1.0`, `AgentMaxClimb=0.25`). Ground = flat BoxShape3D. 0 errors. |
| Flow field prototype | 🚫 | §2, §11 | Deferred to Phase 3. NavServer3D rate-limited to 30 queries/frame handles 1000+ units acceptably. Flow fields are primarily a determinism requirement for lockstep multiplayer — revisit when Phase 3 networking begins. |

### §3 AI Opponent
| Item | Status | GDD Section | Notes |
|------|--------|-------------|-------|
| Rule-based AI skeleton | ✅ | §9 | `AiOpponentSystem`: 3-phase FSM (EarlyEconomy→BuildingBarracks→Training). Supply-cap awareness: builds expansion CC at (54,0,0) when cap−used ≤ 4. Second Barracks at (36,0,-6) once expansion CC is complete. Double production thereafter. `BuildingSystem.TrainUnit()` now enforces supply cap before queueing. Easy/Normal/Hard difficulty controls attack threshold and cooldown. |
| Utility AI decision system | 📋 | §9 | Upgrade from rule-based skeleton |
| Build order logic | 📋 | §9 | |
| Attack timing logic | 📋 | §9 | |
| 2 difficulty levels | ✅ | §10 | `AiDifficulty` enum (Easy/Normal/Hard). Easy: 8 units, 40s cooldown. Normal: 5/25s. Hard: 3/15s. `[Export] AiLevel` on MainScene (Godot Inspector). Supply cap expansion + second Barracks wired in. |

### §3 Base Building
| Item | Status | GDD Section | Notes |
|------|--------|-------------|-------|
| Building placement (footprint) | ✅ | §3 | BuildingStore SoA (64 slots); B key enters building mode, B cycles type (CC/Barracks/ArcheryRange/SiegeWorkshop); ore cost enforced; BuildingBridge renders faction-colored boxes |
| Construction animation/progress | ✅ | §3 | `ConstructionTimer[]`+`ConstructionDuration[]` SoA in BuildingStore; BuildingSystem ticks timer, blocks supply/production until complete; BuildingBridge grows building Y-scale 8%→100%, green glowing progress bar (MeshInstance3D per slot); CommandCardSystem shows "Under Construction Xs (XX%)". 0 errors. |
| Production queues | ✅ | §3 | BuildingSystem.TrainInfantry(): deducts 50 ore, 8-sec timer, spawns unit on complete; CommandCardSystem shows training progress |
| Tech tree (data-driven) | ✅ | §3 | `TechTreeChecker` (src/Core/): static `AreMet()`/`FirstMissing()` checks alive+complete buildings. `prerequisites` string[] on `UnitDefinition`. JSON tree: Barracks→ArcheryRange→SiegeWorkshop. `BuildingSystem.TrainUnit()` blocks if prereqs unmet. `EntityPlacer.PlaceBuilding()` blocks + logs missing prereq. `CommandCardSystem` shows `[need: X]` on train button. `FactionDefinition.GetBuilding()` added. 0 errors. |
| Rally points | ✅ | §3 | `BuildingStore.RallyPoint[]`+`HasRallyPoint[]` SoA. `BuildingSystem.SpawnTrainedUnit()`: issues `UnitCommand.Move` to rally point on spawn if set. `SelectionSystem`: right-click with building selected sets rally point (via `RaycastGround`). `BuildingBridge`: glowing faction-colored pole at rally point position (hidden until set; CommandCenter excluded). 0 errors. |

---

## Phase 2 — Creation Suite & Friends/Family Playtest (Target: Months 9-14)

**EA Definition:** 1v1 online play with trusted testers. Not a public launch.
**Faction 2:** Reskin (same roles, different art/stats). **Terrain:** Terrain3D GDExtension with sculpt + texture paint.

### P2.1 — Foundation
| Item | Status | Notes |
|------|--------|-------|
| JSON scenario format | ✅ | `ScenarioData` + sub-classes (`ScenarioPlayerSlot`, `ScenarioResourceNode`, `ScenarioBuilding`, `ScenarioUnit`). `WinCondition` enum (DestroyAllBuildings / EliminateAllUnits). `[JsonConverter(JsonStringEnumConverter)]`. `resources/data/scenarios/alpha_map_01.json` — 2 CCs, 4 workers, 8 ore nodes. 0 errors. |
| ScenarioSerializer (save/load) | ✅ | `ScenarioSerializer.LoadFromFile()` / `SaveToFile()` — pure C#, `System.Text.Json`, `WriteIndented=true`. Null-safe load (falls back to `ApplyFallbackScenario()`). 0 errors. |
| MainScene loads from scenario file | ✅ | `[Export] string ScenarioPath` on MainScene (Godot Inspector-configurable). `LoadAndApplyScenario()` replaces `SeedMap()`. `ApplyScenario()`: slots→ore+base, nodes, buildings via `PlaceBuildingDirect()`, units via `SpawnScenarioUnit()`. `ApplyFallbackScenario()` mirrors JSON layout. 0 errors. |
| Terrain3D integration | ✅ | Terrain3D v1.0.1 GDExtension installed (`addons/terrain_3d/`). Enabled in `project.godot`. `SetupTerrain()` dynamically instantiates via `ClassDB.Instantiate("Terrain3D")`, imports flat 256×256 RF heightmap at (-128,0,-128) via `terrain.data.import_images()`. Falls back to PlaneMesh if extension missing. NavMesh baking switched to `ParseSourceGeometryData` + `generate_nav_mesh_source_geometry` + `BakeFromSourceGeometryData` (removes static ground body). `NavObstacleManager.Initialize()` now accepts optional `Node3D? terrain`; rebakes use terrain faces + building static colliders. Falls back to `BakeNavigationMesh(false)` if no terrain. 0 errors, 0 warnings. |

### P2.2 — Terrain Editor
| Item | Status | Notes |
|------|--------|-------|
| Brush tools (raise/lower/smooth/flatten) | ✅ | `TerrainBrush.cs` (src/CreationSuite/). Wraps Terrain3DEditor via ClassDB dynamic dispatch. `T` in Edit mode toggles brush. 1-4 = mode (Raise/Lower/Smooth/Flatten). `[`/`]` = resize (5–100u). LMB = paint. Required call sequence: `start_operation(pos)` → `operate(pos, cam_y)` on LMB down; `operate` on each motion; `stop_operation()` (guarded by `is_operating()`) on LMB up. `_store_undo` fires a non-fatal push_error at runtime (no EditorPlugin) — harmless. UI panel: mode buttons + size/strength sliders. `NavObstacleManager.MarkDirty()` on 0.5s debounce. 0 functional errors. |
| Texture painting (3-4 layers) | ✅ | BrushMode.Paint (key 5) — TEXTURE tool + REPLACE operation. `asset_id` in brush_data selects layer 0-3. Layer buttons in UI: Grass/Dirt/Rock/Snow. `SetupTextureAssets()` creates 4 `Terrain3DTexture` slots via ClassDB with solid-colour placeholder albedo textures; falls back gracefully if class unavailable. `terrain.set_camera(cam)` called each frame in `_Process` for correct brush cursor position. No-hit sentinel: `isNaN(hit.Y) \|\| hit.Z > 3.4e38`. 0 errors. |
| Brush UI panel | ✅ | CanvasLayer panel: mode label, 5 mode buttons (1-4 sculpt + 5 paint), layer row (Grass/Dirt/Rock/Snow — visible only in Paint mode), size HSlider (5-100), strength HSlider (1-100). Edit+brush-active visibility only. |

### P2.3 — Editor Tools
| Item | Status | Notes |
|------|--------|-------|
| Palette panel (entity placer upgrade) | ✅ | `EntityPlacer.cs` upgraded: `CanvasLayer` palette at top-right (Edit mode only). Mode row `ButtonGroup` (P1 Unit/P2 Unit/Ore Node/Building). Contextual sub-row: unit archetype buttons or building type buttons. Ghost preview `MeshInstance3D` sibling follows cursor (shape+color per mode). Grid snap `G` key + palette toggle — `SnapValue()` applied to ghost and placement. All keyboard shortcuts (Tab/B/U/G) kept; `SyncPaletteToMode()` keeps palette in sync. 0 errors, 0 warnings. |
| Start position markers | ✅ | `StartPositionBridge.cs` — blue/red flag-pole meshes at P1/P2 base positions. `PlacementMode.StartPos` in palette (5th mode button). Sub-row: [P1][P2] toggle + ore SpinBox. Click terrain to move marker → `MoveStartPosition()` updates `_resources.FactionBase[]` + `_scenario.PlayerSlots[].BaseX/Z`. Flag poles always visible. 0 errors. |
| Win condition setter | ✅ | Edit-mode `PanelContainer` (right side, below brush panel): two `ButtonGroup` radio buttons — "Destroy All Buildings" / "Eliminate All Units" — updates `_scenario.WinCondition`. Win check in `_Process` after 180-frame grace period: `CheckWinCondition()` scans `BuildingStore.Alive/FactionOf[]` or `EntityWorld.IsAlive/FactionOf[]`. On win: `ShowGameOver(winnerPlayer)` sets `_gameOver=true`, shows full-screen `ColorRect` overlay + 56pt "Player X Wins!" label. F5 → Edit dismisses overlay + resets state. 0 errors. |
| Resource node placer | ✅ | Ore Node sub-row now shows Supply SpinBox (100–9999, step 100) + Rate SpinBox (1–20, step 1). `_nodeSupply`/`_nodeRate` fields persist across mode switches. `PlaceResourceNode()` reads configured values. 0 errors. |
| Undo/redo | ✅ | `EditorHistory` (src/CreationSuite/): `(Action redo, Action undo)` pair stack. Ctrl+Z = undo, Ctrl+Y = redo (Edit mode only). All four placement actions wrapped: PlaceUnit (int[] box for free-list-safe redo), PlaceBuilding (Alive flip + ore refund), PlaceResourceNode (Active flip), MoveStartPos (old/new pos+ore via `_slotStartOre[]`). Delete key deletes hovered entity (buildings → units → nodes, priority order) with full undo — building/node undo restores Alive/Active; unit delete snapshots all SoA fields, undo re-creates via `RestoreUnit()`. 0 errors. |

### P2.4 — Multiplayer (1v1)
| Item | Status | Notes |
|------|--------|-------|
| Deterministic sim audit | ✅ | `SimChecksum.cs`: FNV-1a 32-bit hash over entity positions+health, building state, faction ore. `SimulationLoop.EnableChecksums()` + `OnChecksum` delegate fires every 60 ticks. HUD shows live hash. Float audit: no float leakage found in sim tick path — all Fixed, init-time floats only. |
| ENet transport layer | ✅ | `ENetTransport.cs` (src/Multiplayer/): wraps `ENetConnection`, 2 channels (reliable + commands), `HostGame(port)` / `JoinGame(ip, port)` / `Poll()` / `SendReliable()` / `SendCommands()`. Events: `OnPeerConnected`, `OnPeerDisconnected`, `OnPacketReceived`. |
| Command serialization | ✅ | `NetworkCommand.cs`: `PacketType` enum, `UnitOrder` (11-byte struct), `TickCommandPacket.Write/TryRead` (little-endian, max 32 orders), `WriteChecksum/TryReadChecksum`, lobby packets (Hello/Ready/StartGame). |
| Tick synchronization | ✅ | `LockstepManager.Flush(tick)` gates sim advancement: sends local commands, polls transport, returns true only when both peers' tick-N commands have arrived; stalls otherwise. `SimulationLoop.StepOnce()` advances exactly one tick (lockstep-controlled, bypasses accumulator). `MainScene._Process`: online → `Flush()+StepOnce()`; offline → `Update(delta)` unchanged. `SelectionSystem.SetLockstep()` + `EnqueueCommand/EnqueueStationary` helpers route all 4 command types (Move/AttackMove/Stop/Hold) through lockstep when online; pass-through (return true) when null. Path-request delegates on `LockstepManager` (`OnRequestPath/OnRequestAttackMove/OnCancelPath`) bridge to `PathRequestSystem` from `ApplyOrders()` without Godot dep. |
| Desync detection | ✅ | `LockstepManager.SendChecksum()` sends hash to peer; `HandlePacket` compares on receipt; `OnDesync` event fires with tick+both hashes; `GD.PrintErr` logs mismatch. Wired to `SimulationLoop.OnChecksum`. |
| Lobby / match setup screen | ✅ | `LobbyUi.cs` (src/Multiplayer/): Godot CanvasLayer Node, programmatic UI. Port SpinBox + IP field + Host/Join buttons + status label + Ready/Cancel. Lobby state machine: idle→hosting/connecting→connected→ready→match start. `N` key (Edit mode) toggles. `OnMatchStart` event → MainScene switches to Play + calls `_lockstep.GoOnline()`. |
| Stall indicator | ✅ | Amber `PanelContainer` banner centered at top of screen: "Waiting for peer…". Shown only when `_lockstep.IsOnline && _lockstep.IsStalling`. Driven from `UpdateHud()` each frame. Hidden in offline mode and when sim is advancing normally. |

### P2.5 — Content
| Item | Status | Notes |
|------|--------|-------|
| Second faction (reskin) | ✅ | **Iron Pact** — `beta_faction.json` complete (8 units: Forgehand/Footsoldier/Bulwark/Ironclad/Crossbowman/RuneCaster/WarMachine/Wyvern; heavier armor + slower speed vs alpha). `BuildingSystem` upgraded to per-faction `FactionDefinition?[]` — P2 buildings now train P2 units with correct stats. `EntityPlacer` uses correct faction def for P2 unit placement in editor. Art (GLB models) still placeholder (box fallback). No new errors. |
| 5+ 1v1 skirmish maps | ✅ | 6 scenario JSONs: alpha_map_01 (Alpha vs Alpha, symmetric), map_02_iron_crossing (Alpha vs Iron Pact, contested center), map_03_the_narrows (Alpha vs Alpha, tight bounds, rich center nodes), map_04_scorched_plains (Iron Pact vs Iron Pact, sparse ore, low start ore), map_05_crossroads (Alpha vs Iron Pact, N/S bases, EliminateAllUnits + pre-placed scouts), map_06_contested_peaks (Alpha vs Iron Pact, pre-placed Barracks, rich central node). `ApplyScenario` now loads per-slot `faction_json` from scenario file — alpha vs alpha and beta vs beta maps work correctly. `BuildingSystem.SetFactionDef()` allows runtime faction def swap per-slot. |
| Minimap | ✅ | `MinimapBridge` (src/UI/): 200×200 Control anchored bottom-right. `SubViewportContainer` → `SubViewport.World3D` shared with main viewport → orthographic `Camera3D` at Y=200, -90° pitch, Size=256u. **FoW layer**: RGBA8 ImageTexture (128×128, reusable `_fogData` scratch buffer) streamed from `FogOfWarSystem.Grid` each frame via `SetData`+`Update` (1 texture upload/frame, same pattern as FogOfWarBridge): UNEXPLORED=alpha 210, EXPLORED=alpha 110, VISIBLE=alpha 0. **DotOverlay**: blue circles (P1 units), red circles (P2 units), faction-colored squares (buildings). **Click-to-pan**: `MouseFilter=Stop`, `_GuiInput` handles LMB press + drag → `MinimapToWorld(px)` → `_camCtrl.PanTo()`. `RtsCameraController.PanTo(worldPos)` added: clamps to ±128u, sets GlobalPosition XZ. 0 new errors. |

### P2.6 — Pre-Playtest Polish
| Item | Status | Notes |
|------|--------|-------|
| Victory / defeat screen | ✅ | `MatchStats` (src/Core/) tracks per-faction kills via `CombatSystem` + `ProjectileSystem`. `SetupGameOverOverlay` builds a dimming root; `ShowGameOver` populates a `PanelContainer` card at show-time with: VICTORY/DEFEAT 64pt heading (gold/red), "Player N Wins!" in faction color, match duration (MM:SS via `Time.GetTicksMsec()`), per-faction kill tally. All cleared + stats reset on F5→Edit. 0 errors. |
| Score summary | ✅ | `MatchStats` extended: `_unitsBuilt[]` + `_oreMined[]` (int, converted from Fixed via `.ToInt()`). `RecordUnitBuilt(faction)` wired into `BuildingSystem.SpawnTrainedUnit` (after successful `world.Create`). `RecordOreMined(faction, amount)` wired into `GatheringSystem.TickMovingToBase` (before `AddOre`). Both systems accept `MatchStats?` optional ctor param; passed from `MainScene`. `ShowGameOver` card rebuilt: 560×380 card, column-header row ("", "Player 1", "Player 2") + three stat rows (Kills / Units Built / Ore Mined) via `AddStatRow()` local helper — `CustomMinimumSize` on label columns for alignment. Duration still shown above table. Log line updated with all stats. 0 new errors. |

## Phase 3 — Multiplayer (Target: Months 14-20)

### §6 Flow Fields (Deterministic Pathfinding)
| Item | Status | Notes |
|------|--------|-------|
| FlowField data structure + grid math | ✅ | `FlowField.cs` (src/Navigation/): 128×128 grid matching FogOfWarSystem specs. `WorldToCell` uses integer-only arithmetic (deterministic). `Sample(wx,wz)` → FixedVec3 direction. `HasArrived(wx,wz)` → bool (1.5u threshold, same as PathRequestSystem). 0 errors. |
| FlowFieldComputer (BFS pathfinding) | ✅ | `FlowFieldComputer.cs` (src/Navigation/): BFS from goal outward. 8-connected neighbors with precomputed FixedVec3 direction constants (no per-cell sqrt). 3×3 seed region around goal (handles targets adjacent to obstacles). Scratch arrays reused per call (zero GC). Pure C#, no Godot dep. 0 errors. |
| FlowFieldSystem (obstacle map + cache) | ✅ | `FlowFieldSystem.cs` (src/Navigation/): `bool[]` obstacle map (3×3 cell footprint per building = 6×6u clearance). `RebuildObstacles(BuildingStore)` for scenario load. `SetBuildingObstacle(pos, bool)` for incremental updates. `GetOrCompute(goal)` → cached FlowField (same goal cell → same field for all units). Cache invalidated on any obstacle change. Pure C#, no Godot dep. 0 errors. |
| FlowFieldBridge (drop-in PathRequestSystem replacement) | ✅ | `FlowFieldBridge.cs` (src/UI/): `RequestPath/RequestAttackMove/CancelPath` mirror PathRequestSystem API. Per-entity `FlowField?[]` tracking. `_Process`: samples field, sets `MoveTarget = pos + dir * 3u`, checks arrival → Move→Stop (AttackMove left to CombatSystem.ResumeAttackMove). Direct-steer fallback when dir=Zero (unit in goal cell). 0 errors. |
| Wire FlowFieldBridge into MainScene | ✅ | `FlowFieldSystem` + `FlowFieldBridge` fields added to `MainScene`. Bridge created in `SetupNavigation()`, initialized after `LoadAndApplyScenario()` (so obstacle map is seeded with scenario buildings). `SelectionSystem.Initialize` now accepts `FlowFieldBridge?` instead of `PathRequestSystem?`. `LockstepManager` delegates (`OnRequestPath/OnRequestAttackMove/OnCancelPath`) wired to `FlowFieldBridge` in `OnMatchStart()`. `FlowFieldBridge._Process` polls `BuildingStore.Alive[]` each frame and calls `RebuildObstacles` on any change (mirrors `NavObstacleManager` pattern — handles place, destroy, and editor undo/redo). `PathRequestSystem` kept in scene tree (unused) as fallback. 0 new errors. |
| Determinism validation (LAN test) | 📋 | Run LAN test with FlowFieldBridge active. Verify checksums stay in sync through ~300 ticks. NavServer3D desync should be eliminated. |

### §6 Input Delay Buffer
| Item | Status | Notes |
|------|--------|-------|
| Input delay buffering in LockstepManager | ✅ | `INPUT_DELAY=4` ticks (133ms at 30 Hz). Circular command buffers: `_localBuf`/`_remoteBuf` flat arrays `[BUFFER_SIZE * MAX_ORDERS]`, indexed by `tick & BUFFER_MASK`. `GoOnline` pre-seeds ticks 0..3 as empty on both sides. `Flush(T)` sends commands for `T+INPUT_DELAY`, executes commands for `T`. `TickCommandPacket.Write` overload added for flat-array base offset. 0 new errors. |
| Adaptive delay (dynamic N) | 📋 | Measure actual RTT and scale INPUT_DELAY up/down dynamically. Deferred until online testing gives real latency data. |

### §6 Replay System
| Item | Status | Notes |
|------|--------|-------|
| ReplayRecorder | ✅ | `ReplayRecorder.cs` (src/Multiplayer/): pure C#. Streams `(tick, faction, orderCount, orders[])` records to binary `.chmr` file. Only ticks with ≥1 order are written (empty ticks cost nothing). Magic=`CHMR`, version=1 header includes scenario path for playback matching. EOF sentinel `0xFFFFFFFF`. `Close()` is idempotent. |
| ReplayPlayer | ✅ | `ReplayPlayer.cs` (src/Multiplayer/): pure C#. Loads full record set into `Dictionary<uint, List<…>>`. `Flush(tick)` applies stored orders to `EntityWorld` (same logic as `LockstepManager.ApplyOrders`, fires `OnRequestPath/OnRequestAttackMove/OnCancelPath` delegates). Always returns `true` — no stalling. `IsFinished` set when tick ≥ `_lastTick`. |
| LockstepManager recorder hook | ✅ | `public ReplayRecorder? Recorder` field on `LockstepManager`. In `Flush()`, after applying both peers' orders, calls `Recorder.RecordTick()` for local + remote faction. Zero overhead when null. |
| MainScene wiring | ✅ | `[Export] string ReplayPath` — set in Inspector for playback. `StartRecording()` auto-starts on every `OnMatchStart()`; writes to `user://replays/{timestamp}_1v1.chmr`. `StopRecording()` called in `ShowGameOver()` and on F5→Edit. `TryLoadReplay(path)` loads player + wires FlowFieldBridge delegates + enters Play mode. `_Process` adds replay branch (before online/offline). `◉ REC` / `▶ REPLAY` label anchored top-right of HUD. 0 new errors. |

### §6 Command Validation (Anti-cheat Tier 1)
| Item | Status | Notes |
|------|--------|-------|
| Faction ownership check in LockstepManager | ✅ | `ApplyOrders(buf, baseIdx, count, expectedFaction)` — silently drops any order targeting a unit not belonging to the commanding faction. Remote faction computed as `LocalFaction == P1 ? P2 : P1`. Applied to both local and remote order buffers. `ReplayPlayer.ApplyOrders` patched identically. 0 new errors. |

### §6 Dedicated Server (Relay)
| Item | Status | Notes |
|------|--------|-------|
| ServerTransport | ✅ | `ServerTransport.cs` (src/Multiplayer/): multi-peer ENet wrapper. `MAX_SLOTS=4` (0–1 = players, 2–3 = spectators), `MAX_PLAYERS=2`. Per-slot send + broadcast helpers. `BroadcastCommandsToSpectators()`, `IsSlotConnected(int)`. All sends use reliable delivery. `ENetPacketPeer.Flags` type avoided (NuGet SDK compat). 0 errors. |
| DedicatedServer | ✅ | `DedicatedServer.cs` (src/Multiplayer/): Godot Node. State machine: Waiting→OneConnected→BothConnected→BothReady→InGame. Lobby: sends `Hello(faction)` to player peers; `Hello(Neutral)` to spectator slots (no state-machine effect). On both players Ready → broadcasts `StartGame`. In-game: relays `TickCommands` to other player + all spectators (validates faction claim), transparently relays `Checksum`+`DesyncAlert`. `CountConnectedPlayers()` helper counts only slots 0–1. |
| Hello packet faction extension | ✅ | `MakeHello(faction=Neutral)` and `TryReadHello()` added to `NetworkCommand.cs`. `LobbyUi` reads faction byte from Hello: if non-Neutral uses it as `_assignedFaction` in `FireMatchStart` (server-assigned). Falls back to host/client derivation for P2P. Backward-compatible — old 3-byte Hello still parses (faction defaults to Neutral). |
| Headless detection in MainScene | ✅ | `_Ready()` checks `DisplayServer.GetName()=="headless"` / `OS.HasFeature("dedicated_server")` → adds `DedicatedServer` node, returns early (skips all visual init). Port parsed from `OS.GetCmdlineUserArgs()` `--port N`. |

### §6 Competitive Maps
| Item | Status | Notes |
|------|--------|-------|
| map_10_mirror_lake | ✅ | Diagonal bases (-60,-40)/(60,40), 9 ore nodes, 900-supply nexus at center, start ore 250, bounds 130, DestroyAllBuildings, 3 workers each |
| map_11_blitz | ✅ | Close bases (-32,0)/(32,0), 5 contested nodes, pre-built Barracks both sides, start ore 400, EliminateAllUnits, Scout vs Footsoldier opener |
| map_12_the_frontier | ✅ | Far bases (-80,0)/(80,0), 13 nodes with 5-node center cluster (nexus 1000 supply rate 8), start ore 150, bounds 160, DestroyAllBuildings |

### §6 Spectator Mode
| Item | Status | Notes |
|------|--------|-------|
| FogOfWarBridge RevealAll | ✅ | `RevealAll` property; `_Process` fast-path pushes all-255 R8 texture each frame → fully transparent fog. Resets to false on F5→Edit. |
| LockstepManager spectator support | ✅ | `IsSpectator` property. `GoSpectate()` pre-seeds both command stream arrival flags. `Flush()` spectator path: skips send step, waits for both P1+P2 arrivals. `HandlePacket(TickCommands)` routes by faction byte: P1→`_localBuf`, P2→`_remoteBuf`. `EnqueueOrder` returns false immediately when `IsSpectator`. |
| ServerTransport spectator slots | ✅ | Slots 2–3 are spectator slots. `BroadcastCommandsToSpectators()` relays all TickCommands to connected spectators. `IsSlotConnected(int)` helper. |
| DedicatedServer spectator relay | ✅ | `HandleConnect` routes slot≥2 to spectator path: `Hello(Neutral)`, no state-machine effect. `RelayTickCommands` adds `BroadcastCommandsToSpectators()`. `HandleReady` ignores spectator slots. |
| MainScene spectator branch | ✅ | `OnMatchStart`: `Faction.Neutral` → `GoSpectate()` + `_fogBridge.RevealAll=true` + "SPECTATING" HUD label. Skips `StartRecording()`. `_fogBridge` promoted to field. |

### §6 Nakama Matchmaking Integration
| Item | Status | Notes |
|------|--------|-------|
| NakamaClient NuGet package | ✅ | `NakamaClient 3.13.0` added to godot.csproj |
| NakamaService (auth + matchmaking) | ✅ | `NakamaService.cs` (src/Multiplayer/): pure C# (no Godot dep). Email/password auth (`AuthenticateEmailAsync`, creates account on first login). Device ID auth (`ConnectDeviceAsync`) for dev/LAN testing. `FindMatchAsync()` adds to Nakama matchmaker (2-player, `game=chimera_1v1` property). `OnMatchmakerMatched`: determines Player1/Player2 by lexicographic userId comparison, fires `OnMatchFound(MatchFoundInfo)`. Thread-safe `ConcurrentQueue<Action>` for background→main-thread marshaling; caller drains via `DrainEvents()` in `_Process`. `CancelSearchAsync()` removes from queue. `DisconnectAsync()` cleanup. 0 errors. |
| LobbyUi Online tab | ✅ | `LobbyUi.cs` extended: two-tab toggle ("Direct (LAN / IP)" / "Online (Matchmaking)"). Online tab: email + password fields → "Find Match" / "Cancel Search" buttons. `ConnectAndSearchAsync()` async fire-and-forget. `HandleNakamaMatchFound`: auto-calls `_transport.JoinGame(info.ServerIp, info.ServerPort)` → existing peer-connected/Hello/Ready/StartGame flow continues unchanged. `_onlineModeActive` flag suppresses P2P host→Hello send. `DrainEvents()` called in `_Process`. 0 new errors (pre-existing ENetTransport error only). |
| MainScene Nakama config exports | ✅ | 5 `[Export]` fields on `MainScene`: `NakamaHost`, `NakamaPort`, `NakamaKey`, `GameServerIp`, `GameServerPort`. Passed to `LobbyUi` in `SetupMultiplayer()`. Configure via Godot Inspector — no recompile needed to point at production VPS. |
| Nakama server setup | ✅ | `docs/server-deploy/docker-compose.yml` — nakama 3.22.0 + postgres 14. `docs/server-deploy/README.md` — VPS provisioning guide (DigitalOcean/Hetzner, Ubuntu 22.04, firewall ports, systemd service, client Inspector config). `NAKAMA_SERVER_KEY` env var override. Port map: 7350 HTTP, 7777 UDP ENet. |
| Scenario content hashing | ✅ | `ScenarioSerializer.ComputeFileHash(path)` — FNV-1a 32-bit over file bytes (streaming, 4KB chunks). `TickCommandPacket.MakeReady(uint scenarioHash)` — 5-byte packet (type + hash). `TryReadReady(buf, len, out uint hash)` — parse. `LobbyUi.ScenarioHash` property set from `MainScene._Ready()` after `SetupMultiplayer()`. On Ready packet receipt: hash != 0 && mismatch → "MAP MISMATCH" status, `_peerReadyConfirmed=false`, match blocked. Hash displayed as `0x{hash:X8}` in Ready status. 0 new errors. |
| In-game match chat | ✅ | `PacketType.Chat = 0x20`. `MakeChat(faction, msg)` 4+N byte packet (UTF-8, max 200 bytes), `TryReadChat()`. `DedicatedServer` broadcasts Chat to all peers. `LockstepManager.OnChatReceived` event + `SendChat(msg)` (online-only, spectator-safe). `MatchChatOverlay.cs` (src/UI/): layer=8, bottom-left panel, RichTextLabel BBCode log (max 12 msgs, faction-colored), hidden LineEdit input row. Enter=open/send, Escape=close. Own messages echoed optimistically. Auto-hide after 8s. System messages from MainScene ("Match started", "Player X wins! GG"). 0 new errors. |
| Determinism validation (LAN test) | 📋 | Run LAN test with FlowFieldBridge active. Verify checksums stay in sync through ~300 ticks. NavServer3D desync should be eliminated. |

## Phase 4 — UGC Pipeline (Target: Months 20-25)

### §7 Content Package Format
| Item | Status | Notes |
|------|--------|-------|
| ContentPackageManifest | ✅ | `ContentPackageManifest.cs` (src/Core/Definitions/): JSON-serializable metadata class. Fields: id (slug), display_name, description, author, version, min_game_version, tags[], player_count, scenario_file, thumbnail_file, faction_files[], created_at (UTC ISO-8601), scenario_hash (FNV-1a integrity). |
| ContentPackager (pack/unpack) | ✅ | `ContentPackager.cs` (src/Core/Definitions/): `Pack(scenarioPath, outputZipPath, PackOptions)` → creates .chimera.zip (manifest.json + scenario.json + optional thumbnail.png + factions/). `Unpack(zipPath, extractDir)` → extracts + verifies scenario_hash. `ReadManifest(zipPath)` → reads manifest without full extraction (for content browser). `ScanDirectory(dir)` → enumerates all .chimera.zip in a folder. `Slugify(name)` for ID generation. Uses `System.IO.Compression.ZipFile`. 0 errors. |
| Editor Export/Import buttons | ✅ | Win condition panel extended: Map name + Author LineEdits + "Export .chimera.zip" / "Import .chimera.zip" buttons + status label. `ExportMapPackage()`: saves scenario JSON first, packs to same directory as scenario file. `ImportMapPackage()`: Godot `FileDialog` → `DoImport()` → unpacks to `user://imported_maps/{id}/` + copies scenario to `res://resources/data/scenarios/{id}.json`. 0 new errors. |
| In-game content browser UI | ✅ | `ContentBrowserPanel.cs` (src/UI/, CanvasLayer layer=10): scans `user://packages/` via `ContentPackager.ScanDirectory()`. Scrollable card list — map name, author, version, player count, description, colour-coded tag badges, hash fingerprint. "Load Map" button: `Unpack` → copy scenario to `res://resources/data/scenarios/{id}.json` → `GetTree().ReloadCurrentScene()`. "O" key in Edit mode toggles panel (also added to HUD hint). `Refresh()` re-scans on demand. Panel auto-closes on Play mode switch. 0 new errors. |
| mod.io REST API integration | ✅ | `ModIoService.cs` (src/UGC/): pure C# HttpClient wrapper. Browse (GET /games/{id}/mods), streaming download with progress, email auth (request code → exchange → OAuth2 bearer token), upload (create mod entry + upload .chimera.zip file), subscribe/unsubscribe, rate (+1/-1). ConcurrentQueue DrainEvents() pattern for main-thread safety. `ContentBrowserPanel` upgraded to two-tab layout (Local | Online mod.io). Online tab: auth section (email + code exchange), search, scrollable mod cards with Download + Subscribe + Rate buttons. Local cards get "Upload to mod.io" when logged in. `[Export] int ModIoGameId` + `[Export] string ModIoApiKey` on MainScene — configure in Inspector. ModIoService created in SetupContentBrowser only when both fields are non-empty. 0 errors. |
| Creator profiles | ✅ | `ModIoUser.ProfileUrl` (JSON `profile_url`) added to response model. Online browser cards show author name as a `LinkButton` that calls `OS.ShellOpen(profileUrl)` to open `https://mod.io/u/{username}` in the system browser. Falls back to plain Label when URL is empty. 0 errors. |

## Phase 5 — Polish & 1.0 (Target: Months 25-31)

### §9 / §10 Polish & Infrastructure
| Item | Status | Notes |
|------|--------|-------|
| Settings system (camera, audio, accessibility) | ✅ | `SettingsData.cs` (src/Core/Definitions/) — JSON-serializable settings: camera speed + zoom multiplier, edge scroll on/off, master/SFX/music volume, show minimap, show FPS, colorblind mode. `SettingsManager.cs` (src/UI/): Node singleton, loads `user://settings.json` on `_Ready`, `Apply()` pushes values to AudioServer buses + fires `OnSettingsChanged` event. `SettingsPanel.cs` (src/UI/): CanvasLayer layer=15 with sliders (HSlider) + toggles (CheckButton) for all settings, Apply & Save + Reset to Defaults buttons. Escape key toggles panel (wired in MainScene `_UnhandledInput`). Camera seeds `PanSpeedMultiplier`/`ZoomSpeedMultiplier`/`EdgeScrollEnabled` from settings at startup. `SetupSettings()` runs first in `_Ready` so all other systems can read `Current`. 0 errors. |
| Main menu / title screen | ✅ | `MainMenuOverlay.cs` (src/UI/): CanvasLayer layer=20 (topmost). Dark backdrop + WASD-grid aesthetic. Buttons: Play Skirmish (enters Play mode), Create/Editor (Edit mode), Browse Community Maps (opens ContentBrowserPanel), Settings (opens SettingsPanel), Quit. Version string in lower-right corner. Shown on startup; dismissed on mode choice. `SetupMainMenu()` in MainScene after all other setup. `OnPlaySkirmish/OnCreate/OnBrowse/OnSettings/OnQuit` events. 0 errors. |
| Sound / audio system | ✅ | `AudioManager.cs` (src/UI/): POOL_SIZE=8 `AudioStreamPlayer` nodes on "SFX" bus. `TryLoad()` pattern — loads from `res://resources/audio/sfx/` with graceful null fallback when files absent. Drains `CombatEventQueue` each frame → `melee_hit.ogg` / `ranged_hit.ogg` / `explosion.ogg` / `unit_killed.ogg`. ±8% pitch randomisation on hit sounds for variety. Public helpers: `PlayBuildingPlaced()` / `PlayTrainingComplete()` / `PlayUiClick()`. Wired in `MainScene.SetupAudio()` (called after `SetupSettings()`). Volume controlled by existing SettingsManager "SFX" bus. 0 errors. |
| Worker-placed buildings (in-game construction) | ✅ | P1 workers can build buildings in Play mode. `UnitCommand.Build` added to `EntityWorld` with `BuildTarget[]` SoA. `GatheringSystem` skips `Build`-state workers. `BuildingSystem.QueueWorkerBuild()` checks prereqs+ore, places building under construction, issues walk command to worker. `BuildingSystem.TickWorkerArrival()` clears Build command when worker arrives. `CommandCardSystem` worker card: title/HP/status + 4 build buttons (CC/Barracks/ArcheryRange/Siege) with live cost+prereq labels. `OnWorkerBuildRequested` event → `MainScene.EnterBuildPlacementMode()`: translucent green ghost mesh tracks cursor, `_Input` override intercepts LMB (confirm), RMB/Esc (cancel), `RaycastFloor()` for Y=0 intersection. HUD shows "Placing [Name] — Left-click to place…" in controls strip. 0 errors. |
| Natural language trigger scripting (LLM) | 📋 | Phase 5 GDD item — AI-powered trigger authoring |
| AI-assisted map generation | 📋 | Phase 5 GDD item |
| AI balance analysis tools | 📋 | Phase 5 GDD item |
| Performance optimization pass | 📋 | Phase 5 GDD item |
| Advanced editor features (particles, sound triggers) | 📋 | Phase 5 GDD item |
| Linux build support | 📋 | Export template only — no code changes needed |
| 1.0 release | 📋 | Final milestone |

---

## Session Log (Most Recent First)

| Date | Focus | Accomplishments | Next Session |
|------|-------|-----------------|--------------|
| 2026-04-16 | **Phase 5 — Worker construction system + UI bug fixes** | **Worker construction** (5 files, 0 errors): `UnitCommand.Build`+`BuildTarget[]` SoA in EntityWorld; GatheringSystem skips Build workers; `BuildingSystem.QueueWorkerBuild()` (prereq+ore check, place under-construction building, issue walk command), `TickWorkerArrival()` (proximity clear + resume gather), `GetBuildingCost()`/`GetBuildingPlacePrereq()`; `CommandCardSystem` worker card with 4 build buttons (CC/Barracks/ArcheryRange/Siege), live cost+prereq states, "Building [name]…" status; `MainScene`: `_Input` placement intercept, ghost mesh, `EnterBuildPlacementMode()`, `RaycastFloor()`, HUD placement hint. **UI fixes**: Terrain brush panel repositioned to (10,155) below HUD; `IsOverPanel()` guard stops terrain paint on slider clicks; `ApplyBrushSettings()` in `ContinuePaint()` for live slider updates; `SettingsPanel` gets intermediate `anchorRoot` Control (MouseFilter=Stop) for full-screen input blocking; Escape handling moved from `_UnhandledInput` to `_Input` to beat MainScene's toggle handler. **mod.io walkthrough**: `docs/modio-setup-guide.md` — Inspector wiring, test flow, upload/download, troubleshooting table. | Drop `.ogg` files in `res://resources/audio/sfx/`; VPS provision + LAN test; P0.3 Iron Pact art |
| 2026-04-15 | **Phase 5 — Audio system** | `AudioManager.cs` (src/UI/): pool of 8 `AudioStreamPlayer` nodes on "SFX" bus. `TryLoad()` silent-fallback pattern — all sound files optional. Drains `CombatEventQueue` each frame for MeleeHit/RangedHit/SplashHit/UnitKilled. ±8% pitch variation on hit sounds. Public one-shots: `PlayBuildingPlaced()`, `PlayTrainingComplete()`, `PlayUiClick()`. Wired into `MainScene.SetupAudio()` after settings init. Volume lives on existing "SFX" bus (SettingsManager). Asset drop-in path: `res://resources/audio/sfx/`. 0 errors. | Add `.ogg` files to res://resources/audio/sfx/ to hear sounds; VPS provisioning; LAN test; P0.3 Iron Pact art |
| 2026-04-14 | **UI fix + build repair** | Entity palette no longer overflows: redesigned `BuildPaletteUi()` with anchor-based right-edge pinning (`AnchorLeft=AnchorRight=1f, OffsetLeft=-420, GrowHorizontal=Begin`) + `HFlowContainer` for unit sub-row (wraps instead of clips). HUD redesigned: compact 3-line info panel (FPS/mode/tick, unit counts, placement/selection state) + context-sensitive controls strip at bottom-left. `dotnet build` repaired — DLL was stale since Apr 9 due to 8 accumulated API errors: `ENetPacketPeer.FlagReliable` cast to `int`, `ConnectToHost` (not `ConnectTo`), `CanvasItemDefaultTextureFilter` (not `Canvas2DTexSmooth`), `ScrollFollowing` (not `ScrollFollowingEnabled`), `LayoutPreset.Center` (not `CenterCenter`), `AddThemeColorOverride` outside initializer, `PlayerSlots.Length` (not `.Count`), `ProjectChimera.Multiplayer.DedicatedServer` (not `Multiplayer.`). Build: 0 errors, 6 pre-existing CS8632 warnings. LEARNINGS.md updated with all API patterns. | VPS provisioning; LAN test (P2P + dedicated server); mod.io REST API; P0.3 Iron Pact art |
| 2026-04-14 | **Phase 4 — In-game content browser UI** | `ContentBrowserPanel.cs` (src/UI/, CanvasLayer layer=10): scans `user://packages/` via `ContentPackager.ScanDirectory()`. Scrollable card list — map name, author, version, player count, description, colour-coded tag badges per tag array, scenario hash fingerprint. "Load Map" button: unpacks to `user://imported_maps/`, copies scenario + faction JSONs to `res://resources/data/`, updates `ScenarioPath`, calls `GetTree().ReloadCurrentScene()`. "O" key in Edit mode toggles (also wired into HUD hint text). Panel auto-closes on Play mode switch. MainScene: `_contentBrowser` field, `SetupContentBrowser()`, `HandleLoadMap(zipPath)`. 0 new errors (pre-existing ENetTransport CS0426 only). | Drop a .chimera.zip in user://packages/ and test browser; mod.io REST API |
| 2026-04-14 | **Phase 3 — In-game match chat** | `PacketType.Chat=0x20` + `MakeChat`/`TryReadChat` helpers. `DedicatedServer` broadcasts chat to all peers. `LockstepManager.OnChatReceived` event + `SendChat()`. `MatchChatOverlay.cs` (layer=8, bottom-left, BBCode log, hidden input row, Enter/Escape, 8s auto-hide, system messages). `MainScene` creates overlay in `SetupMultiplayer`, initializes on match start, closes on F5→Edit. 0 new errors. | LAN test; VPS provision; P0.3 Iron Pact art |
| 2026-04-14 | **Phase 3 — Content hashing + VPS deploy** | `ScenarioSerializer.ComputeFileHash()` FNV-1a 32-bit. `MakeReady(scenarioHash)` 5-byte packet. Mismatch → lobby blocks match start with hex diff. `MainScene._Ready()` computes + logs hash after scenario load. `docs/server-deploy/docker-compose.yml` (nakama 3.22.0 + postgres 14). `docs/server-deploy/README.md` (full VPS provisioning + systemd service + client Inspector config). 0 new errors. | LAN test; VPS provision; P0.3 Iron Pact art |
| 2026-04-14 | **Phase 3 — Nakama matchmaking** | `NakamaClient 3.13.0` NuGet added. `NakamaService.cs` (pure C#, no Godot dep): email/password + device-ID auth, `FindMatchAsync()` queues into Nakama matchmaker (2-player, `game=chimera_1v1`), `OnMatchmakerMatched` fires `OnMatchFound(MatchFoundInfo{serverIp, port, faction hint})`, thread-safe `ConcurrentQueue` for background→main-thread event marshaling. `LobbyUi.cs` extended: two-tab toggle (Direct / Online), email+password fields, "Find Match"/"Cancel Search" buttons, `HandleNakamaMatchFound` auto-joins dedicated server and resumes existing Ready/StartGame flow, 5 Nakama config `[Export]`s on `MainScene`. Pre-existing ENetTransport error only; 0 new errors. | Provision VPS with Nakama docker-compose; LAN test (P2P + dedicated server); P0.3 Iron Pact art |
| 2026-04-14 | **Phase 3 — Spectator mode** | Full spectator mode across 5 files: **FogOfWarBridge** `RevealAll` branch (all-255 R8 texture fast-path). **LockstepManager** `IsSpectator`/`GoSpectate()`/spectator `Flush()` path/faction-aware `HandlePacket` routing (`_localBuf`=P1, `_remoteBuf`=P2)/`EnqueueOrder` early-return. **ServerTransport** `MAX_SLOTS` 2→4, `MAX_PLAYERS=2`, `BroadcastCommandsToSpectators()`, `IsSlotConnected()`. **DedicatedServer** spectator connect path (`Hello(Neutral)`), relay to spectators in `RelayTickCommands`, `HandleReady` guard, `CountConnectedPlayers()`. **MainScene** `OnMatchStart` spectator branch + `_fogBridge` field promotion + `RevealAll` reset on Edit. Build clean (pre-existing ENetTransport error only). | P2.4 LAN test (P2P mode); dedicated server LAN test; P0.3 Iron Pact art |
| 2026-04-14 | **Phase 3 — Dedicated server + anti-cheat** | `ServerTransport.cs` (multi-peer ENet, slot→faction, broadcast). `DedicatedServer.cs` (state machine, Hello with faction assignment, command relay with faction validation, transparent checksum relay). `NetworkCommand.MakeHello(faction)` + `TryReadHello()`. `LobbyUi` reads server-assigned faction from Hello. `MainScene._Ready()` headless branch — adds DedicatedServer node, skips visual init. Command validation (faction ownership) in `LockstepManager.ApplyOrders` and `ReplayPlayer.ApplyOrders`. 0 new errors (pre-existing ENetTransport Flags error unchanged). | P2.4 LAN test; run headless server + 2 clients |
| 2026-04-14 | **Phase 3 — Replay system** | `ReplayRecorder.cs` (pure C#, streams `(tick, faction, orders[])` to binary `.chmr` file, only non-empty ticks, EOF sentinel). `ReplayPlayer.cs` (pure C#, loads by-tick dict, `Flush(tick)` applies stored orders to EntityWorld + fires FlowFieldBridge delegates, always returns true). `LockstepManager.Recorder` field — records after every executed tick, zero overhead when null. `MainScene`: `[Export] ReplayPath`, auto-record on `OnMatchStart()` to `user://replays/`, replay branch in `_Process`, `◉ REC`/`▶ REPLAY` status label. 0 new errors. | P2.4 LAN test (replays will help diagnose any desync); P0.3 Iron Pact art |
| 2026-04-14 | **Phase 3 — Input delay buffer** | `LockstepManager` rewritten with circular command buffers (`BUFFER_SIZE=16`, `BUFFER_MASK=15`). `INPUT_DELAY=4` ticks. `Flush(T)` sends commands for `T+INPUT_DELAY`, executes buffered commands for `T`. `GoOnline` pre-seeds ticks 0..3 as empty. `TickCommandPacket.Write` overload added (flat-array + base offset). `NetworkCommand.cs` unchanged wire format. 0 new errors. | P2.4 LAN test; P0.3 Iron Pact art |
| 2026-04-14 | **Phase 3 — Wire FlowFieldBridge into MainScene** | `FlowFieldBridge` fully wired: `FlowFieldSystem`+`FlowFieldBridge` fields in `MainScene`; initialized post-scenario-load with `RebuildObstacles`; `SelectionSystem` updated to accept `FlowFieldBridge?` (replaces `PathRequestSystem?`); `LockstepManager` delegates wired to bridge in `OnMatchStart()`; bridge polls `BuildingStore.Alive[]` each frame for obstacle map updates. 0 new errors. NavServer3D `PathRequestSystem` kept as unused fallback. Next: P2.4 LAN test — checksums should stay in sync through 300+ ticks with flow fields active. | P2.4 LAN test; P0.3 Iron Pact art |
| 2026-04-14 | **Phase 3 kickoff — Flow field pathfinding core** | Implemented full flow field pathfinding system (4 files, 0 errors): `FlowField.cs` (grid data + deterministic WorldToCell + Sample/HasArrived), `FlowFieldComputer.cs` (BFS from goal, 8-connected, precomputed Fixed direction constants, reused scratch arrays), `FlowFieldSystem.cs` (obstacle map + field cache, RebuildObstacles + SetBuildingObstacle + GetOrCompute), `FlowFieldBridge.cs` (drop-in PathRequestSystem replacement with same public API). Fixes the NavServer3D non-determinism that causes lockstep desync. Next: wire FlowFieldBridge into MainScene (replace PathRequestSystem, hook building events). | Wire FlowFieldBridge into MainScene; validate determinism on LAN |
| 2026-04-14 | **Session summary — P2.6 polish + minimap complete** | Phase 2 fully code-complete. Shipped: **P2.6 victory/defeat screen** (`MatchStats` kill tracker → styled PanelContainer card with VICTORY/DEFEAT heading, duration, kill tally); **P2.5 minimap** (`MinimapBridge` SubViewport + DotOverlay); **P2.6 score summary** (`MatchStats` extended with UnitsBuilt+OreMined, wired into `BuildingSystem`+`GatheringSystem`, tabular card with 3 stat rows); **Minimap polish** (RGBA8 FoW overlay streamed from `FogOfWarSystem.Grid`, click-to-pan via `_GuiInput`+`RtsCameraController.PanTo()`). All 0 errors. | P2.4 LAN test (two machines); P0.3 Iron Pact art (external); Phase 3 planning |
| 2026-04-14 | Minimap polish — FoW overlay + click-to-pan | **FoW overlay**: RGBA8 `ImageTexture` (128×128, reusable scratch buffer) from `FogOfWarSystem.Grid`; UNEXPLORED=opaque black, EXPLORED=dim, VISIBLE=transparent; 1 `SetData`+`Update` call/frame. **Click-to-pan**: `MouseFilter=Stop` on `MinimapBridge`; `_GuiInput` LMB press+drag → `MinimapToWorld` → `_camCtrl.PanTo()`. **`RtsCameraController.PanTo(Vector3)`** added: clamps to ±128u, preserves Y. `SetupMinimap` updated to pass `_fog` + `_camCtrl`. 0 new errors. | P2.4 LAN test; P0.3 Iron Pact art; Phase 3 planning |
| 2026-04-14 | P2.6 — Score summary | **`MatchStats`** extended with `_unitsBuilt[]` + `_oreMined[]`. **`BuildingSystem`** accepts `MatchStats?` ctor param, calls `RecordUnitBuilt(faction)` in `SpawnTrainedUnit`. **`GatheringSystem`** accepts `MatchStats?` ctor param, calls `RecordOreMined(faction, amount)` on deposit. **`ShowGameOver`** rebuilt as a tabular card: column headers + three stat rows (Kills / Units Built / Ore Mined), two faction-colored value columns each, `CustomMinimumSize` for alignment. Card expanded to 560×380. 0 new errors. | P2.4 LAN test; P0.3 Iron Pact art |
| 2026-04-14 | P2.5 — Minimap | **`MinimapBridge`** (src/UI/): `SubViewportContainer` sharing main viewport's `World3D` → orthographic camera at Y=200 covering ±128u terrain. `DotOverlay` nested Control draws faction dots each frame via `DrawDots()` / `QueueRedraw()`: blue circles = P1 units, red = P2 units, colored squares = buildings. Border + dark bg in `_Draw()`. `SetupMinimap()` wired in MainScene after `SetupHud()`. 0 new errors. | P2.4 LAN test; P2.6 score summary |
| 2026-04-14 | P2.6 — Victory/defeat screen | **`MatchStats`** (src/Core/): pure-C# kill tracker with per-faction `_kills[]`/`_losses[]`, `RecordKill(killedFaction, killerFaction)`, `Reset()`. Wired into **`CombatSystem`** (melee kill path) and **`ProjectileSystem`** (direct hit + splash kill paths) via nullable `MatchStats?` ctor param. **`ShowGameOver`** rebuilt: populates a `PanelContainer` card at show-time — 64pt VICTORY/DEFEAT heading (gold/red), faction-colored "Player N Wins!" subheading, match duration (MM:SS from `Time.GetTicksMsec()`), per-faction kill tally columns. Stats + timer reset on F5→Edit. `_matchStartMs` set on first play frame. 0 new errors. | P2.4 LAN test; P2.5 minimap; P2.6 score summary |
| 2026-04-14 | P2.5 — Iron Pact faction + 6 skirmish maps | **`beta_faction.json`** — Iron Pact (heavier armor / slower speed vs Alpha): Forgehand, Footsoldier, Bulwark, Ironclad, Crossbowman, Rune Caster, War Machine, Wyvern + 4 buildings. **`BuildingSystem`** refactored: `_faction` → `_factions[]` array indexed by `(int)Faction`; `GetProductionUnit(type, faction)` + `SetFactionDef(faction, def)` for runtime swaps. **`EntityPlacer`**: `_faction2` + `ActiveFactionDef()` — P2 unit placement uses Iron Pact stats. **6 scenario JSONs**: alpha_map_01 (symmetric), map_02_iron_crossing (Alpha vs IronPact), map_03_the_narrows (Alpha mirror, 90u bounds), map_04_scorched_plains (IronPact mirror, sparse/low ore), map_05_crossroads (N/S bases, EliminateAllUnits), map_06_contested_peaks (pre-placed Barracks, rich central node). **`ApplyScenario`** now reads per-slot `faction_json` + calls `SetFactionDef()` — same-faction mirrors work. `_slotFactionDefs[]` in MainScene replaces hardcoded two-field pattern. 0 new errors. | P2.4 LAN test; P2.6 victory/defeat screen; P2.5 minimap |
| 2026-04-09 | P2.4 — Full multiplayer foundation | **SimChecksum** (FNV-1a 32-bit, entity pos+health+buildings+ore; float audit: zero leakage in sim tick path). **SimulationLoop** gains `EnableChecksums()`, `OnChecksum` delegate, `StepOnce()` for lockstep-gated advancement. **ENetTransport** (2-channel ENetConnection wrapper, host/join/poll/send). **NetworkCommand** (PacketType enum, 11-byte UnitOrder struct, TickCommandPacket write/read, lobby packets). **LockstepManager** (`Flush(tick)` stall gate, `EnqueueOrder()` offline pass-through, `ApplyOrders()` with path-request delegates, checksum exchange + desync detection). **SelectionSystem** wired: `SetLockstep()`, `EnqueueCommand/EnqueueStationary` helpers gate all 4 command types. **`_Process`** online branch uses `Flush()+StepOnce()`; offline unchanged. **LobbyUi** (N key, Host/Join/Ready/Cancel, full handshake state machine, `OnMatchStart` event). **Stall banner** (amber top-center panel, "Waiting for peer…", driven from `UpdateHud`). All compile clean, zero errors. | P2.4 LAN test; then P2.5 second faction JSON |
| 2026-04-09 | TerrainBrush fix + terrain noise audit | `start_operation(pos)` must be called before each `operate()` — previously commented out under a wrong assumption; added it back. `stop_operation()` (guarded by `is_operating()`) on mouse-up. Two categories of permanent runtime noise documented: `NativeCalls.cs` cascade (4× per operate() call — Godot mono looking for engine Linux build-tree source) and `_store_undo: _terrain isn't initialized` (once per stroke on mouse-up — EditorPlugin undo manager not present at runtime). Terrain sculpt/paint works correctly. 0 functional errors. | P2.4: deterministic sim audit |
| 2026-04-09 | P2.3 Resource node placer + Undo/redo | Ore Node sub-row: Supply SpinBox (100–9999, step 100) + Rate SpinBox (1–20, step 1); `_nodeSupply`/`_nodeRate` instance fields. `EditorHistory` pure-C# class (src/CreationSuite/) with `(Action redo, Action undo)` pair stacks. Ctrl+Z/Ctrl+Y (Edit mode only). All placement actions wrapped: unit via int[] box (free-list-safe), building Alive flip + ore refund, node Active flip, start pos via `_slotStartOre[]`. Delete key: scan buildings→units→nodes by proximity, undo via Alive/Active flip or `UnitSnapshot`+`RestoreUnit()`. 0 errors. | P2.4: deterministic sim audit (FNV checksum) |
| 2026-04-09 | P2.3 Win condition setter | Edit-mode panel (right side) with Destroy All Buildings / Eliminate All Units radio buttons. `SetupWinConditionUi()` uses `ButtonGroup`+`ToggleMode` pattern; connects to `GameState.ModeChanged` signal for show/hide. `_Process` checks `_gameOver` flag + 180-frame grace period before calling `CheckWinCondition()`. `ShowGameOver(player)`: `ColorRect` overlay (alpha=0.55) + centered 56pt label. F5→Edit resets overlay. Stored `_uiCanvas` as field (was local in `SetupHud`). 0 errors. | P2.3: resource node placer (configurable supply) |
| 2026-04-09 | P2.3 Start pos markers + brush fixes | Fixed 3 Terrain3D brush bugs: brush data format `[Image, ImageTexture]`, EXR load via `Image.LoadFromFile` (gdignore bypass), `Terrain3DTextureAsset` class name. Removed `start_operation`/`stop_operation` (editor-only). Moved brush panel right-side. Implemented `StartPositionBridge.cs` (flag poles). `EntityPlacer` gains `StartPos` mode (P1/P2 toggle + ore SpinBox). `MainScene` holds live `_scenario` + `MoveStartPosition()` callback. 0 errors. | P2.3: win condition setter |
| 2026-04-09 | P2.3 Palette panel | `EntityPlacer.cs` fully upgraded. Ghost preview: semi-transparent `MeshInstance3D` sibling added via `GetParent()?.AddChild()` in `Initialize()`; shape changes per mode (BoxMesh units/buildings, SphereMesh ore nodes); color by faction/type. Grid snap: `G` key + palette toggle; `SnapValue()=Mathf.Round(v)` applied to both ghost position and `TrySpawnAt()`. Palette UI: `CanvasLayer` at top-right, mode `ButtonGroup`, contextual sub-row (unit archetypes or building types), snap toggle using `Toggled` event for boolean state. `SyncPaletteToMode()` keeps keyboard shortcuts in sync with panel. HUD updated with G hint. 0 errors, 0 warnings. | P2.3: start position markers (flag objects per player slot, configurable starting ore) |
| 2026-04-09 | P2.2 Terrain editor | Sculpt brush tools (Raise/Lower/Smooth/Flatten) + texture painting (Paint mode, 4 layers). `TerrainBrush.cs` (new, `src/CreationSuite/`): wraps Terrain3DEditor via ClassDB dynamic dispatch. `_Input` consumes LMB+1-5+brackets when brush active; `_UnhandledInput` handles T toggle only. `SetupTextureAssets()` creates 4 `Terrain3DTexture` slots via ClassDB with solid-colour placeholder albedo (Grass/Dirt/Rock/Snow). CanvasLayer UI: mode label, 5 mode buttons, layer row (visible only in Paint mode), size/strength HSliders. 0.5s debounce → `NavObstacleManager.MarkDirty()`. Fixed build errors: `Key.Bracketleft` casing, `float.IsInfinity()`. `terrain.set_camera(cam)` called each frame. `OP_REPLACE=4` confirmed. No-hit sentinel: `isNaN(hit.Y) \|\| hit.Z > 3.4e38`. 0 errors, 0 warnings. | P2.3: palette panel (entity placer upgrade) |
| 2026-04-09 | P2.1 Terrain3D | Terrain3D v1.0.1 GDExtension downloaded + installed. `project.godot` plugin enabled. `SetupTerrain()`: dynamic `ClassDB.Instantiate("Terrain3D")`, flat 256×256 RF heightmap import via `terrain.data.import_images()`, PlaneMesh fallback. `SetupNavigation()`: NavMesh baking switched to `ParseSourceGeometryData` + `generate_nav_mesh_source_geometry` + `BakeFromSourceGeometryData` path. `NavObstacleManager` updated: optional `Node3D? terrain` param, `RebakeWithTerrain()`. Duplicate-before-bake pattern for NavMesh. 0 errors, 0 warnings. | P2.2: terrain editor brush tools |
| 2026-04-09 | P2.1 JSON scenario | `ScenarioData` C# classes (ScenarioPlayerSlot/ResourceNode/Building/Unit + WinCondition enum). `ScenarioSerializer` (load/save, System.Text.Json). `alpha_map_01.json` scenario file. `BuildingSystem.PlaceBuildingDirect()`. `MainScene`: `[Export] ScenarioPath`, `LoadAndApplyScenario()`, `ApplyScenario()`, `SpawnScenarioUnit()`, `ApplyFallbackScenario()`. SeedMap() removed. 0 errors, 0 warnings. | Terrain3D integration |
| 2026-04-09 | Combat feedback | `CombatEventQueue` pure-C# ring buffer (256 slots, 4 event types). `CombatFeedbackBridge`: 48 pooled `MeshInstance3D` hit flashes (melee=orange 0.9s, ranged=yellow 0.7s, splash=red 1.8s, kill=white 1.2s), all shrink to zero. `RtsCameraController.SetShake(duration, strength)`: decaying random XZ offset, overridable by stronger shake. Kill events trigger 0.12s / 0.22u shake. 0 errors, 0 warnings. | Flow field prototype (❓ design decision) |
| 2026-04-09 | Phase 2 planning | Flow field deferred to Phase 3 (NavServer sufficient; real motivation is lockstep determinism). Phase 1 declared complete. Phase 2 scoped: Terrain3D, JSON scenario format first, editor tools, 1v1 multiplayer (friends/family EA), reskin second faction, texture painting in terrain editor. STATUS.md expanded with full P2.1–P2.6 breakdown. | P2.1: JSON scenario format schema + ScenarioSerializer |
| 2026-04-09 | Combat feedback | `CombatEventQueue` pure-C# ring buffer (256 slots, 4 event types). `CombatFeedbackBridge`: 48 pooled `MeshInstance3D` hit flashes (melee=orange 0.9s, ranged=yellow 0.7s, splash=red 1.8s, kill=white 1.2s), all shrink to zero. `RtsCameraController.SetShake(duration, strength)`: decaying random XZ offset, overridable by stronger shake. Kill events trigger 0.12s / 0.22u shake. 0 errors, 0 warnings. | Phase 2 planning |
| 2026-04-09 | AI upgrade | `AiDifficulty` enum (Easy/Normal/Hard). AI builds expansion CC when supply tight (headroom ≤ 4), then second Barracks once CC live — doubles production rate. `BuildingSystem.TrainUnit()` now supply-gates before queueing. `[Export] AiLevel` on MainScene. 0 errors. | Combat feedback or flow field |
| 2026-04-09 | Tech tree | `TechTreeChecker` pure-C# static class. `prerequisites` field on `UnitDefinition` + JSON (Barracks→ArcheryRange→SiegeWorkshop chain). `BuildingSystem.TrainUnit()` prereq gate + `GetUnmetPrereq()` for UI. `EntityPlacer` blocks building placement if prereqs unmet. `CommandCardSystem` shows `[need: X]` on train button. 0 errors, 0 warnings. | AI upgrade or combat feedback |
| 2026-04-09 | QoL controls | `RtsCameraController`: `[Export] bool EdgeScrollEnabled = true`; `E` key toggles in `_UnhandledInput`; live ON/OFF shown in HUD. Attack-move rebind: `Key.A` → `Key.Q` in SelectionSystem (A conflicted with WASD pan-left). HUD legend updated. 0 errors. | Tech tree (data-driven) or AI upgrade |
| 2026-04-09 | NavMesh obstacles | `NavObstacleManager` (src/UI/): watches `BuildingStore`, creates/frees `StaticBody3D` footprints on `NavigationRegion3D` per building, re-bakes navmesh synchronously on change. Navmesh changed from hand-built polygon to geometry-baked (StaticColliders, RootNodeChildren, CellSize=1.0). Godot 4.6 C# note: properties are `GeometryParsedGeometryType` + `GeometrySourceGeometryMode` (not `ParsedGeometryType`/`SourceGeometryMode`). 0 errors, 0 warnings. | Tech tree (data-driven) or AI upgrade |
| 2026-04-08 | Splash/AoE damage | `splash_radius` field in UnitDefinition+JSON. `SplashRadius[]` SoA in EntityWorld. `ProjectileStore.SplashRadius[]` + param to `Spawn()`. `ProjectileSystem.ApplySplash()`: O(n) scan on hit. Siege Engine: 3u radius. All spawn paths wired. 0 errors. | NavMesh obstacles for buildings or Tech tree (data-driven) |
| 2026-04-08 | Rally points | `RallyPoint[]`+`HasRallyPoint[]` in BuildingStore. SpawnTrainedUnit issues Move→rally on spawn. SelectionSystem: right-click on selected building sets rally point. BuildingBridge: faction-colored glowing pole at rally position. 0 errors. | Splash/AoE damage or NavMesh obstacles |
| 2026-04-08 | AI Opponent skeleton | `AiOpponentSystem` (src/AI/, namespace `ProjectChimera.AI`): 3-phase FSM. Phase 1 EarlyEconomy: wait for 100 ore. Phase 2 BuildingBarracks: spend ore, place Barracks at (36,0,6) near P2 base, wait for construction. Phase 3 Training: queue Scout (first Melee, 75 ore, 6s) every cycle; when ≥5 idle P2 combat units and 25s cooldown expired, issue AttackMove to P1_BASE (-45,0,0) for all idle fighters. Falls back to EarlyEconomy if Barracks destroyed. Wired into SimulationLoop after FogOfWarSystem. 0 errors, 0 warnings. | Rally points OR Splash/AoE damage |
| 2026-04-08 | 6 base archetypes | `train_time`+`vision_range` added to UnitDefinition+JSON. `GetUnitByCategory()` on FactionDefinition. `BuildingSystem.TrainUnit()` replaces `TrainInfantry()` — Barracks→Melee, ArcheryRange→Ranged, SiegeWorkshop→Siege (reads stats+train time from faction def). CommandCardSystem: train button for all 3 production buildings with dynamic unit name/cost/time. EntityPlacer: U-key cycles 7 combat archetypes, HUD shows current type. VisionRange wired on all spawn paths. 0 errors. | AI Opponent skeleton (P2 rule-based: worker→gather→build Barracks→train loop) or Rally points |
| 2026-04-08 | Construction animation | `ConstructionTimer`/`ConstructionDuration` SoA in BuildingStore (CC=15s, Barracks/Archery=10s, Siege=12s). BuildingSystem ticks timer, blocks supply cap + production until complete. BuildingBridge: Y-scale grow animation (8%→100%), green glowing progress bar (MeshInstance3D per slot, anchored left edge). CommandCardSystem: "Under Construction Xs (XX%)" when selected. 0 errors. | 6 base archetypes |
| 2026-04-08 | Projectile system | ProjectileStore + ProjectileSystem + ProjectileBridge. Ranged units (archer/mage/siege, range > 2.5u) now fire tracked yellow spheres; ProjectileSystem resolves damage on hit. StressTest wired. 0 errors, 2 pre-existing warnings. 385 FPS in Edit mode. | Construction animation / progress bar |
| 2026-04-08 | Move command override bug | Fixed: Move→Idle transition in PathRequestSystem caused units to immediately chase enemies after completing a right-click move order. Changed both `AdvanceWaypoints` and `CheckMoveGoalArrival` to transition Move→Stop (hold position) instead of Move→Idle (global chase). 0 errors, 0 new warnings. | Projectile system |
| 2026-04-08 | Context reload (no impl) | Session was a context continuation interrupted before projectile system work began. Files read for planning: EntityWorld, CombatSystem, MainScene, EntityPlacer, FixedPoint, SimulationLoop, UnitDefinition, alpha_faction.json. No code written. | Projectile system |
| 2026-04-08 | Command system bugfixes | Fixed right-click move overriding to combat mid-path: replaced Moving-flag heuristic with goal-proximity check (`CheckMoveGoalArrival`) in PathRequestSystem. Fixed control groups allowing P1+P2 mixing: added `FactionOf[i] != Player1` filter to click-select and box-select. 0 warnings. | Projectile system |
| 2026-04-08 | Command system + UnitDef wiring | UnitDefinition wired to spawning (BuildingSystem, EntityPlacer, MainScene.SpawnWorker read alpha_faction.json). Command system: UnitCommand enum + CommandState/CommandGoal SoA; CombatSystem branches (Idle/Move/AttackMove/Stop/Hold); PathRequestSystem adds RequestAttackMove + CancelPath; SelectionSystem adds S/H/A+click. 0 warnings. | Projectile system |
| 2026-04-08 | Pathfinding + command card | PathRequestSystem (30/frame NavServer3D queries, waypoint tracking, fallback); flat navmesh 240×240; formation grid offsets on multi-unit move; CommandCardSystem (building click at 4u, Train Infantry button with 3 states, CC supply display, MouseFilter=Stop). 0 warnings. | UnitDefinition wired to spawning from alpha_faction.json |
| 2026-04-08 | Rendering bug fixes | Fixed `diffuse_unshaded` shader error (→ `unshaded`); fixed units half-underground (MultiMeshBridge yOffset param, pass unitSize.Y/2); fixed ore nodes underground (ResourceNodeBridge Y=0.7f). 0 warnings. | NavigationServer3D pathfinding |
| 2026-04-08 | Building system | BuildingStore SoA, BuildingSystem (supply cap recalc + production queue), BuildingBridge (MultiMesh per type/faction), EntityPlacer B-key building mode, MainScene wired. Supply cap now dynamic from buildings. 0 warnings. | NavigationServer3D pathfinding |
| 2026-04-08 | Fog of War + Supply Cap | FogOfWarSystem (128×128 byte grid, circle stamping, 3 cell states), FogOfWarBridge (R8 texture upload, blend_mix shader overlay), VisionRange SoA in EntityWorld, SupplySystem + ResourceStore supply arrays wired. 0 warnings. | Building placement |
| 2026-04-08 | Resource system | ResourceNodeStore, ResourceStore, GatheringSystem (full state machine), ResourceNodeBridge, EntityPlacer worker/node modes, MainScene wired + seeded. 0 warnings. | Supply cap, fog of war |
| 2026-04-08 | Control groups | Ctrl+1-9 assign, 1-9 recall; dead units pruned on recall; active group shown in HUD + multi-label. 0 warnings. | Phase 1 systems |
| 2026-04-08 | Box select + move command | SelectionSystem rewritten: multi-unit HashSet, drag box-select, R-click move order, 64-ring pool, "N selected" label. MainScene HUD updated. 0 warnings. | Control groups |
| 2026-04-08 | P0.3 import pipeline | UnitDefinition + FactionDefinition C# data classes, alpha_faction.json (8 units + 4 buildings), MeshLoader (GLB→Mesh with box fallback), AssetPreviewScene (turntable + arrow cycling), MainScene wired to faction JSON. 0 warnings. | Generate actual assets in Hunyuan3D / Tripo |
| 2026-04-08 | Steering behaviors | MovementSystem upgraded: seek + arrive (slow within 4u) + separation (SpatialHash, 2u radius, strength 2.5). SpatialHash gains QueryRadius method. Build clean. | P0.3: AI art pipeline evaluation (Hunyuan3D / Tripo) |
| 2026-04-08 | P0.2 + Stretch | All P0.2: RtsCameraController (WASD/edge-scroll/zoom/rotate), GameState (F5 toggle), EntityPlacer (click-spawn), MainScene, GLSL grid terrain. Stretch: SelectionSystem (click-pick, yellow ring, HP bar overlay). All build clean. | Begin P0.3: AI art pipeline evaluation |
| 2026-04-07 | Session housekeeping | CLAUDE.md updated to include LEARNINGS.md in /start protocol; all context files verified accurate | Begin P0.2: RTS camera, terrain placeholder, click-to-spawn, Play/Edit toggle |
| 2026-04-07 | Save/context refresh | LEARNINGS.md populated; CONTEXT.md and STATUS.md confirmed accurate; new /save skill now writes LEARNINGS.md | Begin P0.2: RTS camera, terrain placeholder, click-to-spawn, Play/Edit toggle |
| 2026-04-07 | P0.1 complete + spatial hash | Combat system (DamageMatrix, CombatSystem, SpatialHash); validated 350 FPS at 1000 units; P0.1 fully done | Begin P0.2: RTS camera, terrain placeholder, click-to-spawn, Play/Edit toggle |
| 2026-04-07 | P0.1 | FixedPoint, EntityWorld, SimLoop, MultiMesh bridge, MovementSystem, stress test scene — all compiling | Run stress test in Godot, profile FPS |
| 2026-04-07 | Setup | GDD complete, project scaffolding in progress | Begin P0.1 implementation |